# PLAN — cycle 05 r3 — 2026-09-26
Revision note: r3 replaces r2 at the 4e re-review halt (STATUS 2026-09-26 01:26, Owner action 3) and changes no scope. It gives the owner's GO for 4e (R108) and cuts the halts the owner does not need (R109, R110): the owner asked on 2026-09-26 why the programme keeps stopping, and the answer is that r1/r2 left too many findings and choices returning to him. Owner ruling of 2026-09-26: "go ahead with recommendation". Everything else is carried from r2 unchanged. r2: r2 replaces r1 at the 4e review halt (STATUS 2026-09-25 18:45, Owner actions 1 and 2) and adds one small step. It answers Owner action 1 (R104: a weekly lesson whose tutor is no longer bookable is cancelled at charge time, never charged) and Owner action 2 (R105: fix loop 1 as STATUS proposed), sets 4e's remaining advisor consultations (R106), and adds step 6 `cp/4f-fake-gateway` (R107) so rehearsal can actually book and auto-charge on the fake gateway before CP5 — the planner's bug: r1's R88 claimed that without seeing the 3c DECISION (CYCLE-LOG 2026-09-23 19:20) that no gateway is bound outside tests. R101 also omitted the bookable-tutor check the 4e review found — the planner's bug too. Programme end becomes step 7. Owner ruling of 2026-09-26: "go ahead with recommendation". Everything else is carried from r1 unchanged. r1: first plan of cycle 05. Cycle 04 closed at END (CP3; CYCLE-LOG 2026-09-25 12:52). This cycle is **CP4 — recurring weekly slots — plus the gateway-independent auto-charge half of CP5, pulled forward onto the fake gateway (R88)**, run as one autonomous programme in six steps. It also sets the owner's session-clearing rule (R86), withdraws r9's R82 and corrects R81's evidence (R87 — the planner's bugs), and carries the CP3 leftovers (R103). Rulings R1–R85 carry unless restated or superseded here; R82 is withdrawn; cycle 04's programme rulings (R50, R55, R57, R61, R62) are restated for this programme as R89–R93. R95–R102 are product and design rulings: the planner's recommendations, adopted on the owner's instruction of 2026-09-25 ("make comprehensive plan"); the owner may overrule any of them before `update`.

Standing (every sub-cycle): suite green with the literal count before every push (`COMPOSER_PROCESS_TIMEOUT=1200`); `git log origin/main..HEAD` quoted before every push; each sub-cycle lists its ADVISOR entries with the R63 model line, or states none was triggered; the programme runs across auto-compactions without stopping; **no stop, halt or Owner action is ever raised only to clear the session (R86)**; temporary scripts deleted everywhere and said so; STATUS.md rewritten at every step boundary, push, merge and halt, header carrying tests, advisor counts, review verdict and `Context: <n>k at write`; docs-only commits carry `[skip ci]`, code commits and branch pushes never do. Non-interactive flags (R16); ten-minute stall rule; Herd owner-operated (R18); no browser automation by CC (R20); R30 re-entry rule on every form touched; R41's `/current` path rule on every server command; R75 log timestamps from `date` at write time; RTL logical properties only; every email to the account holder, never a learner (invariant 7).
Advisor: minimum 3 per sub-cycle — design before the first edit, once mid-build, before the PR — plus the mandatory triggers (HOW-WE-WORK rule 11). **Every money-touching decision gets a consultation of its own**: the slot price freeze (R95), per-lesson freezing at generation, the payments uniqueness change and idempotency key (R101), HOLD on auto-charge, retry arithmetic, and R99's cancellation-state mapping. Every ADVISOR entry carries the R63 model line; on the first consultation of each session, confirm the advisor actually answered.
Authorisation boundary: CC may create and push `cp/4a-…` … `cp/4f-…`, open PRs, run fresh-subagent reviews, merge under R89, write docs-only commits to `main`, write DATA_MODEL.md v1.5, CHECKPOINTS.md and DECISIONS.md as R92 lists, edit `CLAUDE.md` **only** as R86(d) describes, edit `app/Services/Payments/*` (frozen only after CP5) as R100–R101 describe, bind the fake gateway in a service provider only as R107 describes, and run the CLAUDE.local.md read-only commands against trustutor-rehearsal. CC may NOT merge 4e without the backend-dev GO (R90); deploy or run a remote migration (R91); run `recurring:generate`, `recurring:charge` or any other state-changing command on a server; change `app/Services/Lessons/LessonStateMachine.php` except under R94; edit anything under `.claude/` (the owner's); touch `docs/brand/`; edit PRD.md or HOW-WE-WORK.md; build the CP5 remainder R88 lists; integrate a real gateway or video provider; create real identities; send real email; or use a browser.

## Rulings (owner decisions, numbered, carried across revisions)
R86 **Session clearing — owner ruling 2026-09-25, standing for this project.** Supersedes HOW-WE-WORK v1.2 rule 13 ("one cycle per session … only at the cycle's END") and §15 where they differ. (a) CC never stops, halts or raises an Owner action for the purpose of clearing. A cycle or programme boundary with more authorised work is not a stop; compaction count and context size are never stop reasons. (b) A clear is offered only at a halt that already needs the owner: a merge/review GO, a question or ruling, a Deploy, a blocker, or programme END when no further step is authorised. At every such halt CC writes STATUS.md handoff-grade (§5 states what is done, what is next and what was ruled out), logs a HANDOFF entry with the measured context and the number of compactions this session, and ends that halt's Owner action with the exact reply that answers it, in the form "then `/clear` this session and reply `update — <answer>`". The owner may skip the clear; the next session must resume correctly either way. (c) Because the answer must survive a clear, every Owner action states the exact reply text that answers it; a bare `update` never answers a question. (d) In 4a's PR, CC replaces rule 13 of `CLAUDE.md`'s `### Owner loop` block with: "13. Never stop only to clear. Offer a clear only at a halt that already needs the owner (GO, question, Deploy, blocker, or END with nothing further authorised): write STATUS.md as a handoff (§5: done, next, ruled out), log HANDOFF with context size and compactions, and end the Owner action with the exact reply, as \"then `/clear` this session and reply `update — <answer>`\". Otherwise run every authorised step without asking for a clear. You cannot clear yourself; the owner does it." — the only `CLAUDE.md` edit authorised — and records ADR-012. HOW-WE-WORK.md stays unchanged until the owner issues v1.3.
R87 **r9 corrections — the planner's bugs.** (a) R82 is withdrawn: `!` bash mode does not run in the desktop Code tab the owner uses (STATUS 2026-09-25 12:10 §6 item 3). Replacement, standing: if Claude Code's auto-mode classifier refuses a read-only command that is on CLAUDE.local.md's allow-list, CC stops (no retry, rephrasing or substitute read), logs a BLOCKER, and raises one Owner action giving the exact JSON string to add to the `permissions.allow` array in `.claude/settings.local.json` — the fix that let `ledger:verify` through on 2026-09-25 — ending "then reply `update — allow rule added`". The owner is never asked to run a server command or paste output. (b) R81's timing was wrong: the planner VM's clock is UTC and the owner's machine is UTC+4, so the allow rule was added at 23:59 local, eight minutes after the 23:51 refusal, and R81's conclusion that option 1 "did not get the call through" was false — it most likely did. STATUS §6 items 2 and 3 are answered by this and come off §6. (c) Planner-side, binding: any file time the planner cites names its clock.
R88 **Programme "CP4+" scope.** CP4 in full per CHECKPOINTS, plus the weekly-slot money path pulled forward from CP5 onto `FakePaymentGateway`: `payment_methods` with a fake saved card, `saveCard`/`chargeSavedCard` on the driver interface, `recurring:charge` with retries, cancellation on the final failure, slot pause and counter reset, and the HOLD on success. Why: without it a weekly slot on rehearsal is a row that never becomes a paid lesson, and nothing in this half needs D-02 or D-04 (R52 set the precedent by pulling the ledger into CP3). **Stays in CP5 (needs D-02 gateway, D-04 entity):** the `payment_gateways` registry and its Filament resource, the Stripe driver and webhooks, real card capture, expired-card detection, the receipts page with VAT and the tax-receipt email, the tutor earnings buckets page, payouts, `settle()` and goodwill, the nightly `ledger:verify` alert. CHECKPOINTS' CP4 "no money yet" becomes "money on the fake gateway only" (R92).
R89 **Merge rule (R50 restated for this programme).** Each sub-cycle: branch `cp/4<x>-<slug>` from current `main` → PR with the sub-cycle's acceptance boxes as its checklist → fresh-subagent adversarial review → fix loop (cap 2) → merge under the rule → post-merge record on `main` → read-only smoke (`composer test` on `main`, count quoted; `curl` on `/`, `/login`, `/admin/login`) → next sub-cycle, without stopping. **Merge rule:** no open Medium or High; CI green on the PR head; diff touches only the areas the sub-cycle names plus tests, migrations, seeders, factories, routes and resources; no frozen-file change (R94); for 4e, the backend-dev GO (R90). Local and CI migrations are expected; no remote migration happens until step 7 (R91). Any Medium+ finding, rule miss or broken money invariant → PR stays open, STATUS §7 names it "awaiting owner", programme halts (a clear point under R86). **Stop conditions:** smoke failure; a regression the fix loop cannot clear in two rounds; any High security finding; any money invariant broken; a failed auto-compaction; the owner typing `stop`; the resume cap. No context cap, no in-step ceiling. **Resume cap:** 8 automatic continuations after stop conditions, reset to **0 of 8** for this programme. **Board:** STATUS §8, seven rows, state / branch / PR / review verdict / merge hash.
R90 **One human gate: 4e halts before merge for the backend developer**, because it moves money. The PR waits with the review attached; its brief also lists PRs 4a–4d so the backend dev can review the whole programme in one sitting (this is CHECKPOINTS' "Backend dev reviews" for CP4). Findings on already-merged 4a–4d work become a fix sub-cycle, never a revert. The owner relays the GO; if the backend dev is unavailable the owner may give it himself, recorded as such. 4a–4d self-merge under R89.
R91 **No deploy and no remote migration by CC (R61 restated).** trustutor-rehearsal receives this programme once, at step 7, when the owner presses Deploy in Forge; the deploy runs `migrate --force` itself. CC then verifies read-only: `migrate:status`, `horizon:status`, `schedule:list` showing `recurring:generate` (daily) and `recurring:charge` (hourly), HTTPS 200, `ledger:verify` (allow-listed; a classifier refusal follows R87(a)). CC never runs either recurring job on a server; the scheduler does.
R92 **Docs authorised this cycle:** DATA_MODEL.md v1.5 — every column, index and table this programme adds or changes, with a deviation note wherever it departs from v1.4 (`recurring_slots.price`; the active-or-paused slot index; `recurring_slot_skips`; `lessons (recurring_slot_id, starts_at)` unique; `payments` uniqueness and `attempt_no`; `payment_methods` as built; the pause/end cancellation mapping of R99). CHECKPOINTS.md — CP4 boxes as they close, CP4's goal line amended per R88, the CP5 tasks done here marked "done in CP4", CP5's remaining list. DECISIONS.md — ADR-012 session clearing (R86), ADR-013 auto-charge pulled forward (R88), ADR-014 slot price frozen on the slot (R95), ADR-015 payments uniqueness and attempt-numbered idempotency keys (R101). PRD unchanged; any build-vs-PRD difference is written in STATUS §6.
R93 **Per-turn floor (R62 restated):** MCP servers, plugins and skills not needed for this programme stay off in the CC window; simple subagents (search, read, grep) on the cheapest capable model; advisor and review subagents on the stronger one.
R94 **`LessonStateMachine` is frozen (CLAUDE.md) and this programme needs no change to it.** R51 already declared every CP4 edge (`reserved → confirmed`, `reserved → cancelled_payment_failed`, `reserved → cancelled_by_parent`, `reserved → cancelled_by_tutor`). Generated lessons enter `reserved` through the same creation entry point 3c's `BookLesson` uses for `pending_payment`. If CC finds the frozen file must change after all, that is build → prove (red/green) → halt → owner GO per HOW-WE-WORK §11 — a clear point under R86.
R95 **A weekly slot's price is agreed once.** `CreateRecurringSlot` computes the regular price from the tutor's current rate, band-validates it (invariant 6 — creating the slot is the booking) and freezes it on the slot as `recurring_slots.price` (fils). Each generated lesson copies the slot's price and freezes its own `commission_pct`, `commission_amount`, `tutor_amount`, `cancel_window_hours` and grace minutes from settings at generation (invariants 6 and 11, read literally: policy is copied when the lesson row is created). A later tutor-rate or band change affects new slots only; a tutor who wants a new price ends the slot with notice. Weekly lessons are always `regular`, never `trial`.
R96 **Trial prerequisite and admin override.** A slot requires the learner–tutor pair to have a trial lesson in `completed`, `completed_reported` or `settled`, or an admin override. Until CP6 wires completion no live lesson reaches those states, so on rehearsal slots are admin-created: a Filament "Create weekly slot" action on a learner–tutor pair that bypasses only the trial check (every other validation runs, including the saved card), audited with the admin's typed reason. Parent-facing entry points still render and, when the check fails, say the weekly slot becomes available after the trial is completed.
R97 **Time and calendar.** A slot is stored as weekday + `start_time` in the tutor's timezone (the availability timezone), on the hour. Each occurrence is converted on its own date, so it is DST-correct and its UTC time (and a parent's local time) can move across a DST change; tests pin Asia/Karachi → a Dubai parent (the CHECKPOINTS box) and Europe/London across the late-October change. An active **or paused** slot blocks its weekday/time in `SlotCalculator` for every date from `starts_on` up to its end (`ends_on` or `end_effective_on`), beyond `generated_until`. Creation validates against the tutor's availability rules and existing future lessons at that moment; afterwards the slot is a standing reservation — later availability-rule edits neither end nor skip it (only `blocked` exceptions, colliding lessons and tutor unavailability cause skips, R98). `starts_on` respects `booking_min_lead_hours`; `ends_on` is optional.
R98 **Generation and skips.** `recurring:generate` runs daily (scheduler, `onOneServer`, `withoutOverlapping`) and extends each `active` slot to today + `recurring_horizon_weeks`, one transaction per slot, with a DB unique index on `lessons (recurring_slot_id, starts_at) WHERE recurring_slot_id IS NOT NULL` as the idempotency backstop. An occurrence that collides with a non-freeing lesson (either CP3 constraint) or a tutor `blocked` exception, or whose tutor is not `bookable()` (suspended, permit expired), is not generated: it is written to a new table `recurring_slot_skips (id, recurring_slot_id, starts_at utc, reason enum lesson_collision|tutor_blocked|tutor_unavailable, notified_at, timestamps, unique (recurring_slot_id, starts_at))` and the parent is emailed once per row (`notified_at` makes the email idempotent). The slot stays active on a tutor-unavailable skip; the admin decides. Paused and ended slots generate nothing. Each generated lesson gets `next_charge_at = starts_at − recurring_charge_lead_hours` (setting read at generation), so 4e needs no backfill.
R99 **Ending and pausing.** Parent ends: immediate; every future `reserved` lesson → `cancelled_by_parent` (free, no ledger, no strike). `confirmed` lessons are **not** auto-cancelled: they stay, and the end screen lists each with its own §4 cancel action and consequence, so ending a slot never moves money by itself. Tutor ends: `end_effective_on` = today + `recurring_tutor_end_notice_days` (setting read at that moment); `reserved` lessons after `end_effective_on` → `cancelled_by_tutor` with no strike; lessons inside the notice period stay and follow §4 if the tutor skips one (strike only inside 24h — CP3's `SkipLesson` rule). Admin end: as parent end, actor recorded. Pause (`admin`, or `payment_failed` from 4e): generation stops, and every future `reserved` lesson is cancelled free as `cancelled_by_parent` with `cancelled_by_user_id` = the actor (admin or null for the system) and `cancel_reason` = `slot_paused` — the state machine has no admin-cancel state and is frozen (R94), so the actor and reason carry the truth. Resume refills through generation from today. **Advisor consultation required on this mapping before 4b's first edit; if the advisor objects, CC halts with an Owner action rather than inventing a state.** The active-slot unique index is replaced (forward-only migration) by one on `(tutor_profile_id, weekday, start_time, timezone) WHERE status IN ('active','paused')`, so a paused slot keeps its place and resume can never collide; two parents racing the same weekday/time are refused at the DB and translated to a clean message.
R100 **Saved card on the fake gateway.** `payment_methods` per DATA_MODEL (one per account; token, brand, last four, expiry, status; never a card number — invariant 15). `PaymentGateway` gains `saveCard` and `chargeSavedCard(Lesson, Money, PaymentMethod, string $idempotencyKey)`. `FakePaymentGateway::saveCard` returns a fake token chosen from two test cards picked from a list, never typed digits: "Test card — always succeeds" (last four 4242) and "Test card — always declines" (last four 0002); the charge outcome follows the token. The parent "Add a card" page is a fake-driver page: it says "Test mode — no real card is charged", has no card-number or CVC field, and refuses to render or submit when `APP_ENV=production` (rehearsal reports `rehearsal`). CP5 replaces the page with the real driver's capture. A slot cannot be created without an `active` card on the account.
R101 **Auto-charge (4e).** `recurring:charge` runs hourly (`onOneServer`, `withoutOverlapping`): every `reserved` lesson with `next_charge_at <= now()` whose slot is `active`, row-locked and re-checked under the lock. Idempotency key `lesson:{id}:attempt:{n}` — derived from the lesson id as invariant 14 requires, with the attempt number so a real gateway's cached result for a failed attempt cannot block the retry; `n` advances only after an attempt's result is recorded, so running the job twice is a no-op. **Success:** in one transaction, `payments` row captured, `reserved → confirmed`, HOLD through `LedgerService`, slot `consecutive_charge_failures` reset to 0; parent emailed a payment confirmation (not a tax receipt — D-04). **Failure:** a `failed` payments row for that attempt, `charge_attempts` +1, `next_charge_at = starts_at − next offset` from `recurring_retry_hours` ([36, 24]), parent emailed with the retry time. **After the last retry fails:** `reserved → cancelled_payment_failed`, slot counter +1, parent and tutor emailed; at `recurring_pause_after_failures` (2) the slot pauses (`payment_failed`) per R99 and parent and admin are emailed. Replacing the card on a `payment_failed`-paused slot offers the parent a one-click resume. **`payments` uniqueness (the R78 item, decided):** `lesson_id UNIQUE` is replaced by `attempt_no` with `unique (lesson_id, attempt_no)` and a partial unique on `(lesson_id) WHERE status NOT IN ('failed')` — one live payment per lesson; failed attempts are their own rows. `BookLesson`'s idempotency guard must still hold on the partial index; a red/green test proves a second capture on one lesson is refused at the DB. `ledger:verify` green on a local seeded DB with charged, failed, cancelled and paused-slot weekly lessons.
R102 **Emails** (queued; rehearsal's log mailer sends nothing — ADR-007): slot created (parent, tutor); slot ended with its effective date (parent, tutor); occurrence skipped by collision or unavailability (parent); charge succeeded (parent); charge failed with retry time (parent); lesson cancelled for payment failure (parent, tutor); slot paused (parent, admin). All to account holders, never learners.
R103 **CP3 leftovers.** (a) The `composer.lock` drift the `e5d31f1` Forge log warned about is 4a's first item: identify the package(s), then either a targeted `composer update <package> --no-interaction` or, if only the content-hash drifted, a deliberate `composer update --lock --no-interaction`; suite green; the lock in its own commit inside 4a's PR. (b) PR #15 finding 14 stays on the CP8 list unless 4b's Filament work touches `AnonymizeUser`. (c) Owner actions A (branch protection) and E carry, non-blocking. (d) Owner decisions D-02 (gateway), D-04 (legal entity) and D-09 (mail provider) are needed before the CP5-remainder plan, not for this programme; CC lists them once, at programme END, not at every halt.
R104 **Owner action 1 (STATUS 2026-09-25 18:45) answered: option 1 — the 4e Medium.** R101's charge conditions gain the tutor: `recurring:charge` re-checks `TutorProfile::bookable()` (invariant 5) under the row lock before calling the gateway. A due `reserved` lesson whose tutor is not bookable (suspended, permit lapsed, account deleted or anonymised) is cancelled at charge time as `reserved → cancelled_by_tutor` (an existing edge — R94 holds) with `cancel_reason` = `tutor_unavailable`, `cancelled_by_user_id` null (system): never charged, no ledger entry, **no strike**, no counter change on the slot. Parent and tutor are emailed (R102 style, account holders only) — the parent's email says nothing was charged and they can keep the slot or end it. The slot stays `active`, so later weeks stay held until the tutor is reinstated or the parent or admin ends it; `recurring:generate` keeps skipping as `tutor_unavailable` (R98). A lesson already `confirmed` when the tutor becomes unavailable is out of this ruling — it follows the existing tutor-cancel and suspension paths.
R105 **Owner action 2 answered: option 1 — fix loop 1 of 2 on `cp/4e-auto-charge`, bounded to:** R104 with tests (suspended tutor, lapsed permit, deleted tutor account — each cancelled, uncharged, no strike, zero ledger rows, both emails); Low 1 (drop "Nothing was paid and nothing is owed" from the missed-window email); Low 6 (re-check the resume policy under the slot lock in `ResumeRecurringSlot`, with a race test); Low 7 (a real schedule assertion for `recurring:charge`, replacing the name-only test). Then a fresh-subagent re-review. The session edits tracked files under `app/`, `resources/`, `database/` and `tests/` with Claude Code's own file-edit tool, one file at a time — no scripted bulk edits (the 18:41 refusal was on a script). If the classifier refuses a plain edit, CC stops, logs a BLOCKER and raises one Owner action naming the exact refused path and tool, ending "then `/clear` this session and reply `update — edit allowed`"; no workaround. CC's 18:41 withdrawal of the in-loop reading of rule 6 stands: Medium+ or any product-behaviour change returns to the owner; Lows in code may be fixed in-loop only when a plan names them, as here.
R106 **4e advisor consultations, completed before the re-review.** 4e had two consultations against R89's minimum of three, and four money decisions shared one consultation where the Advisor line asks for one each (disclosed by CC, CYCLE-LOG NOTE 18:16). Before the re-review CC consults once on R104's cancellation mapping and email wording, and once on the full fix-loop diff (the pre-PR consultation), each logged with the R63 model line — 4e then shows four. Not a halt and not re-done retroactively for the bundled four; from here each money decision gets its own consultation, as the Advisor line says.
R107 **Fake gateway on local and rehearsal — step 6, `cp/4f-fake-gateway`.** The 3c DECISION (CYCLE-LOG 2026-09-23 19:20) left `PaymentGateway` unbound outside tests, so on rehearsal no single booking and no weekly charge can happen until CP5, and every generated weekly lesson would lapse as `charge_window_missed` (STATUS §6 item 2). Bind `FakePaymentGateway` to `PaymentGateway` in a service provider **only when `APP_ENV` is `local`, `testing` or `rehearsal`**; in every other environment — `production` above all — the interface stays unbound and resolving it fails loudly exactly as now. Every page that books, saves a card or shows a charge on a non-production environment shows "Test mode — no real card is charged" (R100's banner, reused). Tests, red/green: the binding is absent under `production` (resolution throws), present under `rehearsal`; a booking and a weekly charge under `rehearsal` write `payments.gateway = 'fake'`. ADR-016 records it as a stopgap for invariant 16 until CP5's registry replaces it (added to the CP5-remainder list). Mandatory advisor consultation before the first edit (production-affecting). Self-merges under R89 — no backend-dev gate; its PR brief is added to the R90 reading list for the backend dev's next look. After this merges, rehearsal can book, save a test card, generate and auto-charge end to end; step 7's verification says so.
R108 **Owner action 3 (STATUS 2026-09-26 01:26) answered: option 1 — GO 4e as it stands.** The R90 GO is given by the owner himself (recorded as such; the backend developer was not consulted for this merge — PRs #16–#20 stay on R90's reading list for his next look, and anything he finds becomes a fix sub-cycle, never a revert). CC re-confirms CI green on the head, merges PR #20 with `--match-head-commit`, writes the post-merge record on `main`, and continues straight into step 6 without stopping. The re-review's open Low and notes (STATUS §6 item 2 (a)–(c)) go to the CP5-remainder list: the gateway status lookup by idempotency key, excluding `Captured` at the R104 guard, and skipping the tutor mail for a trashed user. STATUS §6 is cleaned in the same write: the stale duplicate item 2 ("no gateway bound outside tests") is answered by R107 and comes off once step 6 merges.
R109 **Fewer halts, standing from r3 — supersedes CC's 18:41 withdrawal where they differ.** The programme halts only for: a named gate (R90 — now spent; the Deploy in step 7; programme END); a High finding of any kind; a broken money invariant (1, 3, 6, 11, 13, 14) or a ledger that does not sum to zero; a change to a frozen file (R94); a failed auto-compaction; the owner's `stop`; the resume cap. Everything else is decided by CC and logged, and the programme continues: (a) **Low** findings, in code or docs, never halt — CC fixes them in the current fix loop when they sit inside the sub-cycle's area, otherwise carries them to the STATUS lists; (b) a **Medium** is fixed in the fix loop (cap 2) with an advisor consultation of its own when it touches money, and halts only if two loops cannot clear it or the advisor advises against the fix; (c) a **product choice** the plan does not settle is decided by CC as the option it would mark "(Recommended)", logged as a DECISION headed "owner may overrule", and listed once in STATUS §7 under "Decided for you — overrule at the next halt"; it halts only if no option keeps every CLAUDE.md invariant; (d) an environment workaround that changes nothing in the product (R110) is logged and used, not halted on. Rule 8's named authorisations are untouched: merge to `main` stays under R89, and deploy, remote migration, real identities, real email and live money stay the owner's.
R110 **`composer test` on this machine, standing.** When `composer test` cannot run as one command (PowerShell resolves `bash` to WSL; Git Bash has no `composer` on PATH — STATUS 2026-09-26 §6 item 1), running its constituent steps in order — `php artisan config:clear`, Pint, PHPStan, `bash scripts/rtl-check.sh`, `php artisan test`, `php artisan ledger:verify --no-interaction` — with the literal test count quoted **is** `composer test` for every gate in this plan and CLAUDE.md. Not a halt, not a §6 item after its first mention. Owner item, non-blocking (listed once at programme END): put Herd's `composer` on Git Bash's PATH so the single command works again.

## Steps
1. `cp/4a-foundation` — [DONE, do not redo — merged `6375469` per STATUS 2026-09-25 §2] R103(a) `composer.lock` first. Migrations (forward-only): `recurring_slots` extended to DATA_MODEL's columns plus `price` (R95) — CC checks local and rehearsal row counts (`db:show --counts`) before choosing nullability for existing rows; the active-or-paused unique index swap (R99); `lessons (recurring_slot_id, starts_at)` unique (R98); `recurring_slot_skips` (R98); `payment_methods` (R100). Models, relations, factories, seeders (one demo weekly slot on a demo pair with a fake card). `SlotCalculator`: paused slots block; ends honoured (R97). `CLAUDE.md` rule 13 per R86(d); ADR-012; DATA_MODEL v1.5 schema entries. Tests: schema tests for every new index (savepoint pattern as `LessonSchemaTest`), the blocking half of CP4 box 2. PR, fresh review — halt: no.
2. `cp/4b-slot-actions` — [DONE, do not redo — merged `496a76d` per STATUS 2026-09-25 §2] `CreateRecurringSlot` (R95–R97, card requirement R100), `EndRecurringSlot` for parent, tutor and admin (R99), `PauseRecurringSlot` / `ResumeRecurringSlot` (admin), Policies; Filament `RecurringSlots` resource: list, view, create-with-override (R96), pause, resume, end — every admin action audited. Tests: CP4 boxes 3, 4 and 5; override audited with reason; price frozen on the slot and unchanged by a later rate or band edit; two-parents race refused at the DB. PR, fresh review — halt: no.
3. `cp/4c-generation` — [DONE, do not redo — merged `dd34988` per STATUS 2026-09-25 §2] `recurring:generate` per R98, scheduled daily; skips table and skip emails; `next_charge_at` set. Tests: CP4 box 1 (twice → no duplicates), box 2 (Karachi UTC times; London across DST), paused and ended slots generate nothing, a skip emails once across two runs, per-lesson policy frozen at generation (R95). PR, fresh review — halt: no.
4. `cp/4d-portal` — [DONE, do not redo — merged `c0e2fc3` per STATUS 2026-09-25 §2] parent: learner page lists weekly slots; "Set up a weekly slot" from the tutor profile and the learner page (weekday/time from the tutor's availability, subject, start date, optional end date, then the fake add-card step R100); end-slot screen per R99; skip on `reserved` occurrences (CP3's `SkipLesson`). Tutor: dashboard/calendar shows slots as fixed blocks; end with notice. Slot created/ended emails (R102). Feature test per flow; RTL grep and `npm run build` green. The trial-report CTA stays CP6. PR, fresh review — halt: no.
5. `cp/4e-auto-charge` — [fix loop 1 done at `f4cc7f9`, re-review 0 Medium/High; **GO given by the owner (R108) — merge and continue to step 6 without stopping**; earlier state: built; PR #20 open at `d5f6209`, CI passing; review 1: 1 Medium (suspended tutor charged) + Lows 1, 6, 7 → fix loop 1 per R104–R105, R106's two consultations, fresh re-review; then halt for the backend-dev GO (R90) with the Owner action reply "then `/clear` this session and reply `update — GO 4e`" (or the backend dev's findings); merge with `--match-head-commit`; post-merge record] R100's interface and fake driver charge half; R101's `payments` migration; `recurring:charge` scheduled hourly; retries, final cancellation, pause and card-replace resume; R102's charge emails. Tests: CHECKPOINTS CP5 box 3 in full (T−48h → `confirmed` + HOLD; failures at 48/36/24 → `cancelled_payment_failed`; second consecutive failure pauses; a success resets the counter), a double run is a no-op, `BookLesson` idempotency still holds, ledger zero-sum after every path, `ledger:verify` on the seeded DB. PR with the review brief covering 4a–4e — **halt: yes, backend-dev GO before merge (R90)**; Owner action reply: "then `/clear` this session and reply `update — GO 4e`" (or the backend dev's findings).
6. `cp/4f-fake-gateway` — R107 exactly: the environment-gated binding, the non-production banner wherever booking, card save or charge appears, the red/green environment tests, ADR-016, CHECKPOINTS CP5-remainder line "registry replaces R107's binding". PR, fresh review — halt: no (self-merge under R89).
7. Programme end and rehearsal deploy — docs-only commit per R92; STATUS §8 complete; Owner action: "press Deploy in Forge on trustutor-rehearsal, then `/clear` this session and reply `update — deployed`"; then read-only verification per R91 — with R107 merged, `recurring:charge` runs against the fake gateway there; 0 weekly slots exist until the team creates one, so the only expectations are the schedule entries, `migrate:status`, Horizon, HTTPS 200 and `ledger:verify`; CP5-remainder carried list and D-02/D-04/D-09 (R103d) in STATUS; programme END — halt: yes (nothing further authorised; the planner writes the next plan).

## Out of scope this cycle
The CP5 remainder (R88); CP6 lesson room, attendance, completion, reports and the trial-report CTA; CP7 messaging, reviews, report button; CP8 disputes, admin hardening, production; everything on CLAUDE.md's NOT-in-v1 list — in particular no packs, credits or subscriptions: a weekly slot is a standing reservation and each lesson is charged individually.

## Carried forward
CP5-remainder plan (after D-02 and D-04): `payment_gateways` registry and Filament resource (replacing R107's environment binding), Stripe driver with webhooks and real card capture replacing R100's fake page, expired-card detection, receipts with VAT and the tax-receipt email, tutor earnings buckets, payouts, `settle()` and goodwill, nightly `ledger:verify` alert, the refund-a-captured-but-expired-lesson item. **D-09 mail provider** (R43) before any CP6 email is believable. **Branding step in the CP6 plan** (R49). `trustutor-production` at CP8 from R41's recipe, with its own `btree_gist` check (R85). CP8 hardening list, unchanged from STATUS 2026-09-25: search pagination (R71), multi-process booking race test (R74), learner-side overlap (R76), the three round-2 PASS-WITH-NOTE observations, the `SuspendTutorForStrikes` reconciliation command, PR #14's five NOTE items, PR #15 finding 14, the sweep's post-lock re-check under real gateway latency (revisit at CP5). Owner items, open and non-blocking: A — branch protection on `main` (the one that matters most while PRs self-merge); E — R53 preview UX (revisit CP8). Closed in cycle 04: see PLAN cycle 04 r9 (commit `4239db7`). CP5 also decides STATUS 2026-09-25 §6 item 4 (idempotency across overlapping runs and a card replaced mid-attempt: key per card, or void the pending attempt) and item 3 (a skip between capture and confirmation leaves a captured payment with no hold — needs the refund path). Planner-side, binding: file reads only in the mounted checkout (R84); file times cited with their clock (R87c); before a plan relies on how something runs on a server, the planner reads the DECISIONs that built it.
