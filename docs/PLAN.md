# PLAN — cycle 04 r4 — 2026-09-23
Revision note: r4 replaces r3 mid-step-2 and changes no scope. It records where 3b stands after the 2026-09-23 13:15 HANDOFF (suite green 1005/4588, branch pushed, PR #12 open, review not run), closes the advisor-model-citation question (R63), makes the session-boundary discipline explicit after two consecutive halts at the context ceiling (R64), settles the recurring `.git/index.lock` procedure (R65) and pins the resume order for the next session (R66). Everything other than R63–R66 and step 2's state text is carried from r3 unchanged. History: r3 replaced r2, which CC never read — the programme was already running on r1 when it was written, and r2's step 1 wrongly bundled protocol work into a sub-cycle that had already merged. That was my error. Sub-cycle 3a is CLOSED (PR #11, merged `b75d6e9`, ten Lows fixed with a test each, 619 tests / 2906 assertions); 3b is in progress. The protocol work from HOW-WE-WORK v1.1 moves into 3b, whose PR already touches `ci.yml` for `ledger:verify`. Do not redo 3a. Cycle 03 closed complete (`main` = `5c701ac`, 607 tests / 2830 assertions, trustutor-rehearsal verified). Cycle 04 is CP3 — booking, trial, cancellation — as an autonomous programme in five sub-cycles, opening with the R42 Lows pass. Rulings R1–R49 carry unless restated. This is the first cycle where money moves, even with a fake gateway, so the money rulings are the acceptance bar, not guidance.
Standing (every sub-cycle): suite green with the literal count before every push (`COMPOSER_PROCESS_TIMEOUT=1200`); `git log origin/main..HEAD` quoted before every push; each sub-cycle lists its ADVISOR entries **with the R63 model line**, or states none was triggered; one sub-cycle is one session's work, closed with a HANDOFF entry (§5) and the clear action (§15) whenever the programme stops; temporary scripts deleted everywhere and said so; STATUS.md rewritten at every step boundary, push, merge and halt, header carrying tests, advisor counts, review verdict and **`Context: <n>k at write` read from `/context`**; docs-only commits carry `[skip ci]`, code commits and branch pushes never do. Non-interactive flags (R16); ten-minute stall rule; Herd owner-operated (R18); no browser automation by CC (R20); R30 re-entry rule on every form touched; R41's `/current` path rule on every server command; R45 console work is the planner's.
Advisor: minimum 3 per sub-cycle — design before the first edit, once mid-build, before the PR — plus the mandatory triggers. **Every money-touching decision triggers a consultation of its own**: price computation, freezing, ledger entries, refund arithmetic, rounding. Every ADVISOR entry carries the R63 model line (the tool does not report the answering model; "configured, not measured" is the compliant form — R58 as amended by R63). On the first consultation of a session, confirm the advisor actually answered.
Authorisation boundary: CC may create and push `cp/3a-…` … `cp/3e-…`, open PRs, run fresh-subagent reviews, merge under the R50 merge rule, write docs-only commits to `main`, edit `.github/workflows/ci.yml` **only** as R59 describes, edit `CLAUDE.md` **only** as R60 describes, and run the CLAUDE.local.md read-only commands against trustutor-rehearsal. CC may NOT merge 3c without the backend-dev GO (R55), deploy anything or run a remote migration (the owner presses Deploy; the migration happens inside that deploy — R61), run a state-changing server command without a per-command "yes", touch `docs/brand/` (R49), edit PRD.md, DATA_MODEL.md beyond R57, or HOW-WE-WORK.md at all, integrate a real gateway or video provider, create real identities, send real email, or use a browser.

## Rulings (owner decisions, numbered, carried across revisions)
R50 Programme "CP3" authorised (§12). Five sub-cycles in order, each: branch `cp/3<x>-<slug>` from current `main` → PR with the sub-cycle's acceptance boxes as its checklist → fresh-subagent adversarial review → fix loop (cap 2) → merge under the rule → post-merge record on `main` → read-only smoke (`composer test` on `main`, count quoted; `curl` on `/`, `/login`, `/admin/login`) → next sub-cycle. **Merge rule:** review has no open Medium or High; CI green on the PR head; diff touches only the areas the sub-cycle names plus tests, migrations, seeders, factories, routes and resources; no change to the files the boundary freezes; and for 3c, the backend-dev GO (R55). Local and CI migrations are expected and do not trip §12's remote-migration stop — no remote migration occurs until step 6 (R61). Any Medium+ finding, any rule miss, or any broken money invariant (R52/R53) → PR stays open, STATUS §7 names it "awaiting owner", programme halts. **Stop conditions:** smoke failure; a regression the fix loop cannot clear in two rounds; any High security finding; any money invariant broken; **`/context` at or above the programme cap**; the owner typing `stop`; the session limit. **Programme context cap: 100k** (§12). At every sub-cycle boundary CC reads `/context`: below the cap it continues; at or above it stops, writes STATUS with §5 "context handoff", logs HANDOFF, updates the board and raises the clear action. Carrying one sub-cycle past the cap is a breach, disclosed by the next session that notices. **Resume cap:** 8 automatic continuations, then halt for owner review. **Board:** STATUS §8, five rows, state / branch / PR / review verdict / merge hash.
R51 The state machine is declared in full, exercised in part. `LessonStateMachine` declares every state and every edge in DATA_MODEL's diagram, including the CP4–CP6 ones (`in_progress`, `completed`, `no_show_*`, `disputed`, `settled`, `provider_failure`), each guarded and each firing its event, with a test per allowed and per forbidden edge. Only CP3's edges get calling actions this cycle: `pending_payment → confirmed|expired`, `reserved → confirmed|cancelled_payment_failed|cancelled_by_parent|cancelled_by_tutor`, `confirmed → cancelled_by_parent|cancelled_by_tutor`, and the two `cancelled_by_parent → refunded|completed_reported` outcomes. An edge with no CP3 action is reachable only from a test. No other code path may write `lessons.status` (PRD §4), and a test asserts the column is never written outside the machine.
R52 The ledger lands in CP3, pulled forward from CP5 deliberately: CP3's acceptance requires a refund path and a tutor-paid path, and those are fiction as status changes alone. `ledger_entries` per DATA_MODEL, append-only (update and delete both throw, proven by tests), with HOLD, RELEASE and REFUND implemented and SETTLE/PAYOUT declared but unused. **Invariant: `sum(amount) WHERE lesson_id = X` = 0 after every transition**, asserted in every money test and by a `ledger:verify` command run in CI. `payments` rows are written for every capture and refund. Tutor balance buckets are derived, never stored.
R53 Money rules, absolute. Every amount is integer fils through `App\\Support\\Money` — no float, no `round()` on money, no arithmetic on a raw money integer outside the value object; a test greps the diff areas for float-typed money. `TutorProfile::trialPrice()` is the ONE place a trial price is computed; the onboarding preview's `100 − pct` rounding is deleted and replaced by a call to it, with a test proving preview and booked price agree across the whole band table including an odd-fils case. Frozen onto the lesson at creation and never re-read from settings: `price`, `commission_pct`, `commission_amount`, `tutor_amount`, `cancel_window_hours`, `student_grace_min`, `tutor_grace_min`. A test changes all seven settings after booking and asserts the outcome is unchanged (CP3 box 5). `commission_amount + tutor_amount = price` exactly, remainder fil to the platform, tested at a price that does not divide evenly.
R54 Deletion decided (the open CP2 question). A learner is soft-deleted only when they have no lesson in a non-terminal state; the attempt is refused with a message naming the blocking lessons. A user is never hard-deleted: deletion anonymises (name, email, phone replaced with a tombstone, credentials cleared, sessions and tokens revoked) and leaves `lessons`, `payments` and `ledger_entries` intact with their foreign keys, because financial history must outlive the person. Soft-deleted learners stay visible on past lessons. Admin action, audited; not exposed to parents in CP3.
R55 Sub-cycle 3c halts for the backend developer before merge — the one human gate in this programme. It computes and freezes money, so the PR waits with the review attached until the owner relays the GO. 3a, 3b, 3d and 3e self-merge under R50. If the backend dev is unavailable the owner may give the GO himself, recorded as such.
R56 Booking limits come from settings, not constants: `booking_min_lead_hours` (seeded 12) and `booking_max_days` (seeded 30) drive `SlotCalculator` and `BookLesson` alike; PRD §4's figures are those seeds, not hard-coded rules. `BookLesson` re-derives from `SlotCalculator` at booking time and re-checks `TutorProfile::bookable()` and permit validity then — search-time validity proves nothing (the carried CP2 item). R34's permit cap applies at booking as well as at offer.
R57 Docs authorised this cycle: DATA_MODEL.md v1.4 (columns CP3 adds or changes, with a deviation note wherever the build differs from v1.3), CHECKPOINTS.md (CP3 boxes as they close; CP4 carried items), DECISIONS.md (ADR-008 ledger pulled into CP3 per R52; ADR-009 deletion policy per R54; ADR-010 HOW-WE-WORK v1.1 adopted per R58–R60). PRD unchanged.
R58 HOW-WE-WORK v1.1 is adopted in full and is already in `docs/HOW-WE-WORK.md` (planner-placed, 443 lines, v1.1 plus seven TrusTutor lines appended to §14 and marked as such). CC commits it unchanged in 3a's docs commit and does not edit it. The three operative changes bind from this cycle: **advisor proof** — every ADVISOR entry names the responding model or it is a tool failure, and the first consultation of the cycle confirms the advisor answered; **HANDOFF** — a new CYCLE-LOG kind, written whenever a session closes at a boundary, carrying the context size, the step closed and confirmation that the channel files carry what is done, what is next and what was ruled out; **`Context:`** — STATUS's header carries the figure read from `/context` at the moment of writing, as a measured fact, never an estimate, and CC never estimates or logs its own token spend.
R59 CI economy, in 3b alongside the `ledger:verify` step, the only `ci.yml` edit authorised this cycle: `paths-ignore` for `docs/**`, `*.md` and `docs/brand/**` so documentation pushes do not build; a `concurrency` group keyed on the ref with `cancel-in-progress: true`; Composer and npm caching. Both land in the same 3b PR and are named in its diff scope. Everything CI currently runs — Pint, the suite on `postgres:18` + `redis`, `npm run build`, the RTL check, the Horizon boot check — stays exactly as it is; `[skip ci]` is a commit-message habit (Standing), not a workflow change.
R60 `CLAUDE.md`'s `### Owner loop` block is replaced, in the 3b PR, with Appendix B of HOW-WE-WORK v1.1 verbatim, with `<repo path>` filled in as `C:\\project elearning`. That is the entire authorised `CLAUDE.md` edit; the Stack line, invariants and conventions are untouched. Rule 13 (one step per session, the clear action) and rule 14 (200k, the 100k ceiling, the programme cap, sessions are free, keep the per-turn floor low) then bind CC directly from its own file rather than only from this plan.
R61 No remote migration by CC, this cycle or any other, and no deploy. trustutor-rehearsal receives CP3 once, at step 6, when the owner presses Deploy in Forge; the Forge deploy script runs `migrate --force` itself. CC's part is read-only verification afterwards. Push-to-deploy stays OFF (R41), so every docs commit on `main` — most of this cycle's traffic — reaches the server never.
R62 Per-turn floor (§15): MCP servers, plugins and skills not needed for CP3 stay off in the CC window for the duration of this cycle. Simple subagents (search, read, grep) run on the cheapest capable model; the advisor and the review subagent always use the stronger one. CC states in 3a's STATUS §3 which of these it could confirm and which are the owner's to set.
R63 Advisor-model citation, decided (closes STATUS §6 "advisor-model-naming gap" and Owner action 2). The advisor tool does not report which model answered a call, so R58's "names the responding model" is satisfied, for every ADVISOR entry from cycle 04 onward, by the fixed phrase **"Model: not reported by the tool; configured advisor per PROJECT_BRIEF is Fable 5.1 — configured, not measured."** An entry carrying that phrase counts toward the sub-cycle minimum and the mandatory money triggers. An entry that names a model as a measured fact without the tool having reported one is the error, not the other way round. The four existing cycle-04 entries (2026-09-21 07:40, 07:50, 06:00 and 2026-09-23 12:30) are accepted as compliant retroactively; no rewrite of past log entries. 3b's advisor minimum is therefore met (design 05:20, mid-build 05:50, pre-PR 06:00 plus the 12:30 consult); the pre-review consult in step 2 below is the one still owed. HOW-WE-WORK.md is not edited for this (planner-owned, R58); CLAUDE.md is not edited for this (R60 boundary). The ruling lives here and in DECISIONS.md as a one-paragraph addendum to ADR-010, written in step 2's post-merge docs commit.
R64 Session boundary, made explicit. Two consecutive halts (12:40 and 13:15) were caused by the same thing: `update` typed into a window that had not been `/clear`ed, so the new session inherited the old one's context and hit the 100k ceiling before doing new work. From now on: (a) the owner's part of the loop is always `/clear` then `update` — never a bare `update` after a HANDOFF; (b) CC's first action on every `update` is to read `/context` (or `get_usage`) **before** `git fetch` or any file read, and if the figure is already above 40k it is a continuation, not a fresh session — CC writes one START line saying so, does not begin a review, a subagent spawn, a suite run or any step that cannot finish inside the remaining budget, writes STATUS §7 with the single line "Owner action: `/clear` this session, then reply `update`" and stops; (c) the fresh-subagent adversarial review is always the first substantive action of a fresh session, never the tail of a long one; (d) the programme resume cap (R50, 8 automatic continuations) counts a rule-(b) stop as a resume — it is now at 2 of 8.
R65 `.git/index.lock` procedure, standing. Three occurrences (2026-09-21 05:00, 2026-09-23 12:29, 13:00), all stale, all zero bytes, all removed safely. Authorised as a standing procedure so it no longer needs disclosure as an own-action each time: if `git` fails on `index.lock`, CC runs `tasklist //FI "IMAGENAME eq git.exe"`; if no `git.exe` is running and the file is 0 bytes, remove that one file and continue, logging a one-line NOTE ("index.lock #n, stale, removed") rather than a full disclosure. If a `git.exe` process IS running, or the file is non-empty, do not remove it — halt with a BLOCKER naming the process. Owner-side likely cause: the Cowork planner seat reads the repo through a mounted folder while CC works in the same checkout; the owner will avoid running planner file reads while CC is mid-`update`. If a fourth stale lock appears after this ruling, STATUS §6 raises it as a Medium process item and the owner investigates the mount.
R66 Resume order for step 2, pinned. On the next `update` (after `/clear`): 1. read `/context` (R64b); 2. `git fetch`, fast-forward `main`, confirm PR #12's head is still `origin/cp/3b-state-machine` and CI on that head is green (quote the run id); 3. commit this PLAN r4 to `main` as a docs-only commit; 4. one ADVISOR consult, pre-review, on the PR #12 diff with the six-point brief from the PR body, logged with the R63 phrase; 5. spawn the fresh-subagent adversarial review per HOW-WE-WORK §8 with the PR body as its brief — the reviewer receives the six points verbatim, including the commit-date mismatch and the `phpunit.xml` line, and returns numbered verdicts with a MERGE-RULE line; 6. fix loop, cap 2, each round re-running the suite with the literal count; 7. merge under R50's rule; 8. post-merge record on `main` (CYCLE-LOG DECISION quoting the rule, CHECKPOINTS.md boxes closed by 3b, DECISIONS.md ADR-010 addendum per R63); 9. read-only smoke on `main` (`composer test` count quoted; `curl` on `/`, `/login`, `/admin/login`); 10. read `/context`: below 100k → write STATUS with the step-3 opening and continue to `cp/3c-booking`; at or above → STATUS §5 "context handoff", HANDOFF, clear action. Steps 4–6 are the new work the last two sessions correctly declined; nothing in 1–3 may be skipped to reach them faster.

## Steps
1. `cp/3a-lows` — [DONE, do not redo] merged as `cp/3a-lows`, PR #11, `b75d6e9`: the R42 Lows pass, ten Lows fixed with a test each plus a red proof; `whereDate` in `bookable()` reviewed against the index with EXPLAIN on 60,000 rows and deliberately left as is. One item was not built — search pagination is still in memory — and it is NOT re-opened here: it is an open owner question in Carried forward, and nothing in CP3 depends on it.

2. `cp/3b-state-machine` — [in progress — built, suite green 1005 tests / 4588 assertions (2026-09-23 13:10), branch pushed, **PR #12 open** (https://github.com/rizwanoor80/eLearning-Platform/pull/12), review NOT run; remaining work is exactly R66 items 4–10] `lessons` per DATA_MODEL including every index and both partial unique indexes (the CP2 stub migration is extended, never replaced); `tutor_strikes`; `ledger_entries` per R52 with the append-only guards and `ledger:verify` wired into CI; `LessonStateMachine` per R51 with the full edge matrix and its tests; `Money` extended only if CP3 needs an operation it lacks, with unit tests. No booking action yet. **Plus the protocol work, named here so it is inside this PR's diff scope:** R59's `ci.yml` edit beside the `ledger:verify` step; R60's `CLAUDE.md` Owner-loop replacement; and a separate docs-only commit on `main` carrying HOW-WE-WORK v1.1 as the planner placed it (443 lines — commit unchanged, do not edit) and ADR-010. PR #12; fresh review — halt: no.
3. `cp/3c-booking` — `BookLesson` per R53 and R56: slot validated through `SlotCalculator`, `bookable()` and permit re-checked, type decided (trial if no prior non-cancelled lesson for the learner–tutor pair), price from `trialPrice()`, band-validated, the seven values frozen, `pending_payment` lesson created, overlap and one-trial-per-pair held by the DB indexes AND a transactional check; `FakePaymentGateway` capturing instantly behind the CP5 driver interface and writing a `payments` row; capture → `confirmed` + HOLD; 15-minute unpaid `expired` sweep; the onboarding preview unified per R53. Tests: CP3 boxes 2, 3 and 5, the odd-fils split, a concurrency test proving the DB constraint under parallel requests, a settings-change-after-booking test. PR #13 — **halt: yes, backend-dev GO before merge (R55)**.
4. `cp/3d-cancellation` — `CancelLesson` and `SkipLesson` per PRD §4 using each lesson's FROZEN window: parent ≥ window → `refunded` + REFUND; parent < window → `completed_reported` + RELEASE; tutor any time → REFUND, strike if inside the window; skip from `reserved` free both ways, strike only for a tutor inside 24h; reschedule = cancel + book under the same rules; `tutor_strikes` rows; 3 strikes in 90 days → tutor `suspended` through the R36 transition table with the admin email. Tests: CP3 boxes 4 and 6, the 25h/23h pair, ledger zero-sum after every path. PR #14; fresh review — halt: no.
5. `cp/3e-dashboards-emails` — parent dashboard (upcoming lessons, cancel action) and tutor dashboard (today and upcoming); the CP3 emails — confirmed, cancelled, skipped — through the settings sender; 24h and 1h reminders as a scheduled job, `onOneServer` + `withoutOverlapping`, idempotent (proven by running the job twice); R54's deletion behaviour with its audit rows. Mail stays `log` (R43), so tests assert queued notifications, never delivery. All CP3 boxes closed; STATUS §8 marks CP3 done. PR #15; fresh review — halt: no.
6. Programme end and rehearsal deploy — docs-only commit per R57; STATUS §8 complete; §7 "Owner action 1: press Deploy in Forge on trustutor-rehearsal, then reply `update`" — then CC verifies read-only (`migrate:status`, `horizon:status`, HTTPS 200, `ledger:verify` clean) and writes the CP4 carried list — halt: yes.

## Out of scope this cycle
CP4 onward: recurring weekly slots, `in_progress`/`completed`/report/dispute actions, real payment gateway, video provider and rooms, payouts, reviews, messaging. Also out: applying the brand pack (R49); the mail provider (R43, D-09); production; Arabic; any Forge or DNS change; any `ci.yml` or `CLAUDE.md` edit beyond R59 and R60.

## Carried forward
CP4 plan: recurring slots and `recurring:generate`; then the completion half of the state machine (join, complete, report, 72h auto-release) on the RELEASE path built here. D-02 gateway and D-04 entity by the CP5 plan. **D-09 mail provider** (R43) before any CP6 email is believable. **Branding step in the CP6 plan** (R49): `docs/brand/theme/colors.css`, the type and radii tokens, front end and Filament panel, favicon/OG/apple-touch set, C and D lockups with the 768px and 480px rules; the designer's vector redraw and heavier small-symbol cut must be in hand before CP8. `trustutor-production` at CP8 from R41's recipe. **Search pagination (open owner question, non-blocking):** the tutor search still pages in memory after the slot check. Recommendation, and CC's: leave it — v1 launches with a handful of tutors, so it costs nothing now — and put the figure on the CP8 hardening checklist with a trigger of a few hundred approved tutors. The alternatives are paging in SQL before the slot check, which changes search behaviour and needs its own plan, or capping the candidate set, which silently hides tutors. No CP3 work depends on the answer. Owner items, open: branch protection on `main` (R7/R23 — the one that matters most now that PRs merge under an autonomous rule; exact GitHub steps in cycle 01's CYCLE-LOG 18:35 BLOCKER); `.claude/settings.local.json` (ADR-002, optional). Closed this revision: the advisor-model citation (R63); `CLAUDE_CODE_DISABLE_1M_CONTEXT` — `/context` has reported 200k in every measured session this cycle, treated as confirmed. Known and disclosed, not to be reconciled: `4d3ed3c`/`9f80bda` carry commit dates (2026-09-20) earlier than the CYCLE-LOG entries describing their design (2026-09-21); the code was independently verified against the pinned design and is in PR #12's review brief. The reviewer may comment on it; it is not a merge blocker on its own. Planner-side: this cycle's planning conversation is retired when cycle 04 closes, and anything argued only in chat goes into PROJECT_BRIEF.md or DECISIONS.md first (§15).
