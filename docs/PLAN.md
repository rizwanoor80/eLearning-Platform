# PLAN — cycle 07 r1 — 2026-09-26
First plan of cycle 07. Cycle 06 closed at END (CYCLE-LOG 2026-09-26 11:20, owner's machine clock; rehearsal runs `1ede299`, 1375 tests). The owner answered every STATUS §7 item and the cycle's scope one question at a time on 2026-09-26 (planner's record: R119–R126). This cycle is programme **"CP6"**: demo tutors on rehearsal, then CP6 in full (lesson room, attendance, reports, trial → weekly slot), then the R49 branding step, then deploy and END. Nothing here needs D-02 or D-04.

Standing (every sub-cycle): all of cycle 05 r4's Standing line carries unchanged (suite green with the literal count before every push, `COMPOSER_PROCESS_TIMEOUT=1200`, R110 constituent steps count as `composer test`; `git log origin/main..HEAD` quoted before every push; R63 model line on every ADVISOR entry; runs across auto-compactions; **no stop, halt or Owner action only to clear (R86)**; STATUS.md at every step boundary, push, merge and halt; `[skip ci]` on docs-only commits only; R16, R18, R20, R30, R41, R75; RTL logical properties only; every email to the account holder, never a learner). Timestamps via the `TS` env var, never `shell_exec('date')` (cycle 06 NOTE 10:46). R109 (fewer halts) and R112 (mid-run revisions) stand.
Advisor: minimum 3 per sub-cycle — design before the first edit, once mid-build, before the PR — plus the mandatory triggers (rule 11). Own consultation each for: the webhook signature check and credential masking (7b), room-creation idempotency and no-show/provider-failure money mapping (7c), release-exactly-once and the 72h auto-release (7e). First consultation of each session: confirm the advisor answered.
Authorisation boundary: CC may create and push `cp/7a-…` … `cp/7g-…`, open PRs, run fresh-subagent reviews, merge under R123, write docs-only commits to `main`, write the docs R126 lists, deploy trustutor-rehearsal under R111 (standing; CLAUDE.local.md line 8 now says so), run R121's seeder once on rehearsal, and run the CLAUDE.local.md read-only commands. CC may NOT: deploy production; run any other state-changing server command; hold, read or ask for Daily, Postmark, Stripe, Forge or GitHub credentials; create a real Daily room with a real key; send real email; change `LessonStateMachine.php` except under R94; edit `.claude/`; build anything from CP5, CP7 or CP8; use a browser.

## Rulings (owner decisions, numbered, carried across revisions)
R1–R118 carry as last written, except R82 (withdrawn). R86, R93, R94, R109–R112 are restated by reference and bind this programme.
R119 **Owner questions are asked one at a time — owner ruling 2026-09-26, standing.** The planner asks each owner question separately in chat, recommended option first with a one-line reason, takes the answer there and then, and writes the answers into PLAN.md as rulings; the owner never has to reply with a bundled "`update — 1: … 2: …`" list. CC's side is unchanged except for this: each STATUS §7 Owner action carries exactly one question (never two decisions in one line), so the planner can put them to the owner one by one. In the first docs-only commit of this cycle CC records this in `docs/HOW-WE-WORK.md` as v1.3: (a) §2 step 2 gains "The planner asks questions one at a time, each with its recommended option first and a one-line reason, takes each answer immediately, and writes the answers into PLAN.md as rulings"; (b) §7's last bullet becomes "The planner asks the owner questions in chat one at a time, recommended option first, and writes each answer into PLAN.md as a ruling; the owner never answers a bundled list"; (c) §7 gains "One question per Owner action line"; (d) Appendix A's "Discuss freely…" bullet gains the same sentence as (a); (e) the header gets a "Changes in 1.3" line. Nothing else in the file changes.
R120 **Owner decisions on D-02, D-04, D-09 (2026-09-26).** **D-09 mail provider: Postmark** (ADR-007 already names it). Wiring it — sending domain, SPF/DKIM in Cloudflare, API key in the admin — is its own later step with the owner; rehearsal stays `MAIL_MAILER=log` (R43) and no plan claims delivery until then. **D-04 legal entity: open**, the owner takes it to counsel; nothing this cycle depends on it. **D-02 payment gateway: multiple gateways**, integrated through CP5's `payment_gateways` registry with keys entered in the admin (PRD §12); **Stripe is the first real driver**, other drivers (PayPal, a UAE-local gateway) follow as separate cycles on request. Stripe is provisional until D-04 confirms account eligibility. No CP5 work this cycle.
R121 **Demo tutors on rehearsal (7a).** `Database\Seeders\DemoTutorSeeder`: at least 6 approved, bookable demo tutors across the launch curricula (GCSE, IB, CBSE) with profiles, subjects, year groups, rates inside the bands, availability rules for the next 8 weeks, and permits valid ≥ 12 months; clearly fictional names, `@example.test` emails, no real identity (rule 9). Idempotent (keyed on the example emails; a second run changes nothing) and it never updates or deletes an existing row; it is not called from `DatabaseSeeder`. Tests: two runs → same counts; existing users untouched. After 7a merges and deploys, CC runs it **once** on rehearsal with the exact command in CLAUDE.local.md line 28 (owner ruling 2026-09-26; the line is the owner's authorisation), then verifies read-only: `db:show --counts` (`tutor_profiles` ≥ 6), `curl` `/tutors` 200 listing them, one profile 200, and `/tutors/{id}/book` → 302 to `/login` for a guest. That closes STATUS §6 item 2g.
R122 **Programme scope — CP6 in full per CHECKPOINTS, plus R49's branding step.** Sub-cycles: 7a demo tutors (R121) → 7b `video_providers` registry, Filament resource, manager, `fake` provider, Daily driver, signed webhook endpoint, `lessons.room_provider` → 7c room lifecycle: create at T−15 (exactly once per lesson), per-party join tokens, join active at T−10, close at end + 10 min, attendance timestamps from webhooks or manual "I've joined", `in_progress → completed` sweep, no-show paths per PRD §4, admin `provider_failure` (ledger refund entry, no strike, no tutor pay, no rating; the gateway `refund()` call stays CP5 per the deferred list) → 7d lesson page for both parties (embed when `supports_embed`, else link; buttons at T−10) → 7e progress reports: form (5 fields, +3 for trial), `SubmitProgressReport`, parent email, `LedgerService::release` exactly once, 72h auto-release with the late flag → 7f trial-report email and portal CTA pre-filled for a weekly slot (with a real completed trial, R96's prerequisite is now met without the admin override), learner timeline and "recent focus areas" → 7g branding per R49: palette, type, radii and lockups from `docs/brand/` on the front end and the Filament theme; no layout or copy changes; R49's binding facts apply. Out: the tldraw whiteboard stretch.
R123 **Merge rule — R89 restated for 7a–7g.** Branch `cp/7<x>-<slug>` from current `main` → PR with the sub-cycle's CHECKPOINTS boxes as its checklist → fresh-subagent adversarial review (authorisation, another parent's lesson or learner, webhook forgery and replay, credential leakage into logs or props, double room creation, double release, timezone edges) → fix loop (cap 2) → **self-merge** when no Medium or High is open, CI is green on the head, and the diff stays inside the sub-cycle's area plus tests, migrations, seeders, factories, routes and resources → post-merge record → smoke (`composer test` on `main` with the count; `curl` `/`, `/login`, `/admin/login`) → next sub-cycle without stopping. Halts only per R109. Resume cap reset to **0 of 8**. Board: STATUS §8, eight rows.
R124 **7e moves money and self-merges (owner, 2026-09-26).** No backend-dev gate: release is a ledger entry through the existing `LedgerService::release`, never a gateway call. It gets the advisor consultations above, `ledger:verify` green and zero-sum after every path in its tests (report, second submit rejected, auto-release, provider failure, both no-shows), and any broken money invariant halts under R109. PRs #16–#22 and 7c/7e join R90's reading list for the backend developer's next look; findings become a fix sub-cycle, never a revert.
R125 **Daily.co key — non-blocking (owner, 2026-09-26).** Everything is built and tested against the `fake` provider and HTTP-faked Daily responses; CC never holds a Daily key. On rehearsal the `fake` row is active until the owner enters the Daily key in the new admin screen. At END, one Owner action: create the Daily account, paste the API key and webhook secret into the rehearsal admin (`Video providers → Daily`), activate it — with the exact screen path. Credentials: encrypted at rest, masked in the form, never in logs, props, STATUS or the repo (invariant 16). `fake` cannot be activated when `APP_ENV=production`.
R126 **Docs authorised this cycle:** HOW-WE-WORK.md v1.3 exactly as R119 lists; PRD §11 decision table rows D-02 ("multiple gateways via the registry; Stripe first, provisional on D-04") and D-09 ("Postmark") only; DECISIONS.md ADR-017 video-provider registry and Daily driver, ADR-018 the D-02/D-09 rulings (R120); DATA_MODEL.md v1.6 for every table and column this programme adds (`video_providers`, room and attendance columns, progress reports, late flag); CHECKPOINTS.md CP6 boxes as they close; STATUS, CYCLE-LOG, reports. Any other build-vs-PRD difference goes in STATUS §6.

## Steps
1. Docs-only commit: R119's HOW-WE-WORK v1.3, R126's PRD rows and ADR-018 — done means: commit on `main`, diff quoted in a VERIFICATION entry — halt: no.
2. `cp/7a-demo-tutors` (R121) → merge → deploy rehearsal (R111) → seeder once → R121 checks — done means: counts and the three `curl` results quoted — halt: no.
3. `cp/7b-video-registry` (R122, R125) — done means: merged; tests for masking, no-log, fake-in-production refusal, webhook signature reject and replay no-op — halt: no.
4. `cp/7c-room-lifecycle` — done means: merged; CP6 boxes 1 and 2 ticked with test names — halt: no.
5. `cp/7d-lesson-page` — done means: merged; CP6 box 6 ticked; RTL check and `npm run build` green — halt: no.
6. `cp/7e-reports` (R124) — done means: merged; CP6 boxes 3 and 4 ticked; `ledger:verify` green on a seeded DB — halt: only per R109.
7. `cp/7f-trial-cta-timeline` — done means: merged; CP6 box 5 ticked (queue, not delivery — R43) — halt: no.
8. `cp/7g-branding` (R49) — done means: merged; before/after screenshots of home, tutor profile, booking and admin login in `docs/reports/7g.md` — halt: no.
9. Deploy and END — R111 deploy of the final `main`; R91 checks plus `schedule:list` showing the room-creation and completion jobs, `/tutors` and a profile 200; END with advisor summary; STATUS §7: R125's Daily action, then the UI developer's review of PRs #22 and 7g on rehearsal (non-blocking) — one question per line (R119) — halt: yes, END.

## Out of scope this cycle
CP5 (registry, Stripe, real capture, receipts, earnings, payouts) and anything needing D-04; Postmark wiring (R120); CP7 messaging, reviews, report button; CP8; the tldraw whiteboard; everything on CLAUDE.md's NOT-in-v1 list.

## Carried forward
CP5 plan after D-04: registry replacing R107's binding (ADR-016), Stripe first (R120), the 4e Lows (R108), the 6a expected-quote check in `BookLesson`, STATUS 2026-09-26 §6 items 2–4. Postmark wiring step (R120, R43). `trustutor-production` at CP8 (R41, R85). The CP8 hardening list as in STATUS 2026-09-26. Owner items, non-blocking: A — branch protection on `main`; E — R53 preview UX (CP8); Herd `composer` on Git Bash PATH (R110). `vue-tsc` error in `weekly-slots/Create.vue:146` (STATUS §6 item 2c) — fix inside 7d or 7f if either touches that area, else CP8.
