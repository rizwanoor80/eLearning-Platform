# PLAN — cycle 01 r3 — 2026-09-17
Revision note: r3 replaces r2 after the owner's rulings on PHP 8.4, PostgreSQL 18 and the permission allow-list; the local services now exist (Herd Pro). r1 steps 1–2 and r2 step 0 are closed (CYCLE-LOG END 18:35; commit `657a2e2` on `origin/main`) — do not redo them. Everything else is restated in full because a revision overwrites, never appends. Execute step 0 first, then from the first unfinished step.
Standing (applies to every step): suite green with the literal count before every push;
`git log origin/main..HEAD` quoted before every push; each step names its ADVISOR entries
or states none was triggered; temporary scripts deleted from every server and said so;
if the session nears its limit, STATUS.md first.
Advisor minimum this cycle: 4 consultations in total — #1 already logged (18:33); #2 native-Windows design and the allow-list syntax before the first code edit (step 0/3); #3 auth boundary (step 4, mandatory); #4 before the PR (step 8).
Authorisation boundary: CC may make the docs-only commit in step 0 and push it to `main`; create `.claude/settings.local.json` per R14 (git-ignored); create local databases on this machine (local only); create and push branch `cp/0-foundation`; open PR #1; run the fresh-subagent review; and — `gh auth status` shows `rizwanoor80` — set the `main` branch protection in R7 via `gh api`. CC may NOT merge PR #1 (owner GO), run any installer that needs an administrator prompt, sign up for or configure any external service other than GitHub, create real identities, send real email, touch Docker Desktop, or run anything outside the checkout and the local services named in CLAUDE.local.md.

## Rulings (owner decisions, numbered, carried across revisions)
R1 D-01 confirmed: Inertia + Vue 3. R2 D-03 confirmed: Daily.co (not touched this cycle). R3 D-06 confirmed: trial discount 50%.
R4 D-07 (final): native Windows via Laravel Herd Pro — no Docker, no Sail, no WSL. **PHP 8.4 everywhere** (local, CI, servers) — the kit's 8.3 is superseded, no fallback logic. Composer and Node from Herd. Services from Herd, auto-started, details in CLAUDE.local.md `## Local services`: **PostgreSQL 18** on 5432 (superuser `root`, no password), **Redis 7** on 6379, **Mail catcher** on 2525 (`MAIL_MAILER=smtp`, `MAIL_HOST=127.0.0.1`, `MAIL_PORT=2525`, `MAIL_ENCRYPTION=null`). Horizon is installed as a dependency so production config and CI are real, but `php artisan horizon` is never run on Windows: the local worker is `php artisan queue:work redis`. `composer install` must succeed on Windows with no manual flags (`config.platform` faking `ext-pcntl`/`ext-posix` plus `platform-check: php-only`, or an equivalent the advisor approves). Reverb is installed and configured; if `reverb:start` cannot boot on Windows, log a DECISION and defer its first run to CP7. Site served via `herd link project-elearning` → `http://project-elearning.test`; if Herd refuses the spaced path, `php artisan serve` is acceptable (DECISION). The checkout stays `C:\project elearning` — quote the path everywhere.
R5 D-08: public repo `https://github.com/rizwanoor80/eLearning-Platform.git`, default branch `main`. The no-secrets / no-real-identity rule is absolute: nothing private is ever committed.
R6 `main` already holds `5179aff`, `accde12`, `14af7d7`, `657a2e2`, `938dba4`. All code goes on `cp/0-foundation` → PR #1 → review → halt for the owner's merge GO. No merge without it.
R7 Branch protection on `main`: require a pull request, require the CI status check, block force-pushes and branch deletion; do NOT require code-owner approval and do NOT enforce for administrators (docs-only commits from this checkout must land on `main` directly per HOW-WE-WORK §8). `.github/CODEOWNERS` = `* @rizwanoor80`.
R8 Tests run against **PostgreSQL 18** — local databases `elearning` (dev) and `elearning_testing` (tests) on the Herd service, a `postgres:18` service container in CI — never SQLite; later checkpoints depend on Postgres partial unique indexes. Local `.env` uses the Herd superuser (local only); `.env.example` carries the same harmless local defaults.
R9 Starter: the official Laravel 12 Vue starter kit (Inertia 2, Vue 3, Tailwind 4, Fortify-based auth) with Pest. Generated third-party UI components under `resources/js/components/ui/` may be excluded from the RTL grep only if they cannot be converted to logical utilities; any exclusion is a DECISION entry and is listed in STATUS.md §4. Every app-authored file is covered by the grep.
R10 Seeded admin credentials come from `.env` keys (`ADMIN_EMAIL`, `ADMIN_PASSWORD`) with placeholder values in `.env.example`; no real email or password anywhere in the repo.
R11 The kit's STATUS.md owner actions 1–5 are closed by R1–R5; D-02 stays open (Carried forward).
R12 Local service credentials live only in `CLAUDE.local.md` under `## Local services` (git-ignored, owner-written) and in `.env` (git-ignored). Read them, never quote a password, connection string or password-bearing URL in any log entry, STATUS.md, commit, report or PR.
R13 Docker Desktop is left alone — not uninstalled, not repaired, not referenced by any file in the repo.
R14 Written allow-list (HOW-WE-WORK §8, "extend CC's access under an explicit written allow-list and record it as an ADR"): CC creates `.claude/settings.local.json` (inside the git-ignored `.claude/`) with permission rules that pre-approve, without the auto-mode heuristic: `git` (status, log, diff, fetch, pull, add, commit, branch, checkout <branch>, switch, push to `origin`), `composer`, `php`, `php artisan`, `vendor/bin/pest`, `vendor/bin/pint`, `npm`, `npx`, `node`, `herd`, `psql`, `gh api`, `gh pr`, `gh run`, `gh issue view`; and that deny `git push --force`/`-f`, `git reset --hard`, `git checkout .`, `git clean`, `git branch -D`, `rm -rf`. Exact rule syntax per the current Claude Code permissions documentation (ADVISOR #2 covers it); a rule that fails to parse is dropped, never widened. The list is recorded verbatim as ADR-002. If the rules only take effect after a session restart, say so in STATUS.md §7 as an owner action.

## Steps
0. Record the rulings (docs-only, on `main`) — done means: one commit pushed to `main` containing: DECISIONS.md row `ADR-002 | 2026-09-17 | Claude Code permission allow-list (settings.local.json) | The auto-mode heuristic refused a docs-only push once (CYCLE-LOG 20:23); the return channel cannot depend on a heuristic | Accepted (owner ruling)` with the R14 list quoted in the Decision cell; PRD §10 stack line → PHP 8.4 / PostgreSQL 18; PRD §11 D-07 status cell → "Decided (owner, 2026-09-17): Herd Pro native, PHP 8.4, PostgreSQL 18, Redis 7, Herd mail catcher — ADR-001/002"; CHECKPOINTS.md CP0 environment line → PostgreSQL 18, PHP 8.4; CLAUDE.md Stack line → `PHP 8.4 · PostgreSQL 18` and the `## Local environment` section rewritten from R4 (at most six lines) — files: docs/DECISIONS.md, docs/PRD.md (two lines), docs/CHECKPOINTS.md (one line), CLAUDE.md — this plan explicitly authorises these edits; commit hash quoted. Then create `.claude/settings.local.json` per R14 and quote `git status --ignored` showing it ignored.
1. Environment check (r3) — done means: quoted output of `php -v` (8.4.x), `composer --version`, `node -v`, `npm -v`, `herd --version` (Herd Desktop running), a successful connection to PostgreSQL 18 on 127.0.0.1:5432 (psql if on PATH, else a PHP `pdo_pgsql` one-liner printing `version()`), Redis `PING` → `PONG` on 6379 (redis-cli if present, else a PHP one-liner), and port 2525 open — halt: only if any of the three services is unreachable → Owner action naming which one, "open Herd → Services/Mail and start it — reply `update` when done".
2. Repo bootstrap — [done in r1: `5179aff`, `accde12`; `CLAUDE.local.md` ignored]. Nothing to do; do not redo.
3. Scaffold on branch `cp/0-foundation` — done means: databases `elearning` and `elearning_testing` created via the Herd superuser (R12); Laravel 12 created in a temporary directory and moved in without touching docs/, CLAUDE.md, CLAUDE.local.md*, .gitignore entries, `.claude/`; `.env` on pgsql 18 + redis + Herd mail per R4; `php artisan migrate` green; Horizon and Reverb installed and configured per R4; Filament installed with a panel at `/admin`; a fresh `composer install` on Windows succeeds with no flags (quoted); `composer test` green with the count; `npm run build` green; site answers HTTP 200 per R4 (quoted) — ADVISOR #2 before the first edit: composer platform config, Horizon and Reverb on Windows, scaffolding into a non-empty folder, RTL-grep strategy, Postgres-only test config, allow-list syntax.
4. Roles and auth — done means: `users.role` enum (`account_owner|tutor|admin`) via `App\Enums\Role`; registration + login + email verification for `account_owner` at `/register` landing on an empty `/dashboard`, and for `tutor` at `/tutor/register` landing on a `/tutor/onboarding` placeholder page; no admin registration route — admin exists only via seeder and logs into Filament; Policies gate the parent area, tutor area and the Filament panel by role; feature tests for each entry point, each landing page and each cross-role denial; one verification email observed in Herd's mail catcher (quoted subject line, no addresses); suite count stated — files: app/Enums, app/Models/User, app/Policies, routes, resources/js/pages — ADVISOR #3 (auth boundary, mandatory).
5. Money + Settings — done means: `App\Support\Money` (integer fils; add, subtract, percentage, format "AED 120.00", no float leakage) with unit tests; `settings` table + `Settings` facade with cache; unit tests for read, write and cache-bust; suite count stated — files: app/Support, app/Services/Settings, database/migrations, tests/Unit.
6. Seeders — done means: curricula (GCSE, A_LEVEL, IB_MYP, IB_DP, CBSE), ≈25 subjects, price bands per curriculum × tier from PRD §3 (only the tiers that exist for that curriculum), settings from DATA_MODEL.md `settings`, admin user per R10; factories for every model created so far; `php artisan migrate:fresh --seed` green on the local `elearning` database only; row counts quoted — files: database/seeders, database/factories.
7. Layout shell + RTL guard — done means: the app layout sets `<html dir>` from the user's locale (always `ltr` in v1) and uses logical utilities only; `scripts/rtl-check.sh` greps `resources/` for `ml-|mr-|pl-|pr-|left-|right-|text-left|text-right` (R9 exclusion rule) and is wired into `composer test` (must run under Git Bash on Windows and on Linux); a VERIFICATION entry quotes it failing on a temporary component using `ml-4` and passing after `ms-4` (temporary component removed and said so) — files: resources/js/layouts, scripts/, composer.json.
8. CI + branch protection + PR — done means: `.github/workflows/ci.yml` on PHP 8.4 runs Pint (`--test`), `composer test` against `postgres:18` + `redis` services, `npm run build`, the RTL check, and one command proving Horizon boots on Linux (e.g. `php artisan horizon:status` against the Redis service) on every PR; `.github/CODEOWNERS` per R7; branch protection per R7 via `gh api` (settings quoted); suite green with the literal count; `git log origin/main..HEAD` quoted; branch pushed; PR #1 opened with the CP0 acceptance list from CHECKPOINTS.md as its checklist; CI green on the PR (run URL quoted) — ADVISOR #4 before opening the PR.
9. Review + halt — done means: fresh-subagent adversarial review of PR #1 logged as REVIEW with numbered verdicts (Low doc-only findings fixed in-cycle with a DEVIATION entry; anything else stops the cycle); STATUS.md rewritten with §7 "Owner action 1: GO to merge PR #1 — reply `update` when done" — halt: yes, owner GO required.

## Out of scope this cycle
Docker in any form; anything from CP1 onward; the tutor onboarding wizard beyond its placeholder page; any external service other than GitHub (no Daily, Stripe, email provider, Forge); hosting or a rehearsal server; Arabic translation; any DECISIONS.md row other than ADR-002.

## Carried forward
Forge rehearsal server (Linux, PHP 8.4, PostgreSQL 18, Horizon for real) as its own cycle right after PR #1 merges — owner ruling 2026-09-17. D-02 gateway and D-04 legal entity: settle by the CP5 plan (backend dev verifies saved-card charging and entity eligibility). D-05 brand and domain: by CP6. Human review gating (backend dev before merge on CP3/4/5, UI dev after merge) to be confirmed in the CP3 plan. Reference spec pack not yet in docs/reference/ — add when convenient; the PRD wins regardless.
