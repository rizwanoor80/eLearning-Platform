# PLAN — cycle 03 r3 — 2026-09-19
Revision note: r3 replaces r2 after the owner's rulings on the brand (R40), the rehearsal server as actually built (R41), the unfixed Lows (R42) and mail (R43). Steps 1–3 are closed and merged (`a6281eb`, `530b5e5`, `6214ad4`; `main` = `50d53ee`). Do not redo them. Step 4's owner actions are now DONE — the server exists and the first deploy succeeded — so step 4 resumes at CC's verification half. Everything else is restated in full because a revision overwrites, never appends. Rulings R1–R39 carry unless restated below.
Standing (applies to every step): suite green with the literal count before every push; `git log origin/main..HEAD` quoted before every push; each step names its ADVISOR entries or states none was triggered; temporary scripts deleted from every server and said so; if the session nears its limit, STATUS.md first. Non-interactive flags (R16); ten-minute stall rule; Herd owner-operated (R18); no browser automation (R20); R30 re-entry rule on every form touched. `COMPOSER_PROCESS_TIMEOUT=1200` on `composer test` (STATUS §6).
Advisor minimum: 2 per step. Step 4 touches a real server: consult before every server-touching judgment call, and treat any unexpected output as a halt, not a thing to fix.
Authorisation boundary: CC may run the read-only commands listed in CLAUDE.local.md `## Servers` against trustutor-rehearsal, write docs-only commits to `main`, and open a PR if step 5 needs one. CC may NOT run anything state-changing on that server without a per-command "yes" (that includes `migrate`, `queue:restart`, cache clears and triggering a deploy), re-run `db:seed` at all, edit Forge configuration, hold Forge/Hetzner/Cloudflare credentials, read or echo the server `.env`, create a production server, create real identities, send real email, or use a browser.

## Rulings (owner decisions, numbered, carried across revisions)
R40 D-05 decided: the brand is **TrusTutor**, domain `trustutor.com` (owner, 2026-09-19). Naming convention, used everywhere from now on so the project is unambiguous among the owner's other Hetzner/Forge projects: Forge servers `trustutor-rehearsal` and later `trustutor-production`; sites `rehearsal.trustutor.com` and later `trustutor.com`; databases `trustutor_rehearsal` / `trustutor_production`; Hetzner private network `trustutor-pn`. Never say "the server" or "rehearsal" alone in STATUS, CYCLE-LOG or a PR — always the full name. The `site.name` setting on trustutor-rehearsal is set to `TrusTutor` in step 4; ADR-006 records the brand in step 5.
R41 Rehearsal server as built (supersedes R38, which said DigitalOcean/Ubuntu 24.04 — my error; the owner used Hetzner): Laravel Forge → Hetzner Cloud CX23, Falkenstein, private network `trustutor-pn`, Ubuntu 26.04, PHP 8.4, PostgreSQL 18 (`trustutor_rehearsal`), Redis, Let's Encrypt active on `rehearsal.trustutor.com` (Cloudflare A record, DNS-only). Repo `rizwanoor80/eLearning-Platform:main`. **Push-to-deploy is OFF** — every deploy is the owner pressing Deploy in Forge, so a docs-only commit to `main` never touches the server. Deploy script = Forge default + `artisan migrate --force` + `$RESTART_QUEUES()`. Horizon is a Forge background process; the scheduler is a Forge scheduled job every minute; both run as `forge`. **Zero-downtime deploys are ON, so the app is at `/home/forge/rehearsal.trustutor.com/current`, not `/home/forge/rehearsal.trustutor.com` — every path on that box carries `/current` or it fails with "Could not open input file: artisan".** Already seeded once; `db:seed` is never run there again. The server details and the two allow-lists live in CLAUDE.local.md `## Servers` (written 2026-09-19, sha256 `6c7073c5…`); CC reads that file, never Forge.
R42 The ten unfixed Lows (STATUS §6, five from step 2 and five from step 3) are **not** fixed in this cycle. They become a named Lows pass at the front of the CP3 plan, ordered worst first: the year-group `withTrashed()` 500 and the `sort` upper bound first (both are admin-facing errors), then the Livewire tier-mismatch, the adult-learner curriculum change, the `Onboarding.vue` index lookup, and step 3's five. Rationale: none sits on a live money path, none is reachable by a parent or tutor in normal use, and folding them into step 4 or 5 would widen a cycle that is closing. They stay listed in STATUS §6 until that pass closes them.
R43 Mail: trustutor-rehearsal runs `MAIL_MAILER=log` for now — **nothing sends, and no email from that box is evidence of anything**. Choosing and wiring a transactional provider is **D-09**, its own step in a later cycle (recommendation when it comes up: Postmark or Resend, one sending domain `trustutor.com`, SPF/DKIM in Cloudflare, sender address from the `mail` settings group so it stays admin-editable per PRD §12). Until D-09 is decided, no plan may claim an email path is verified end to end; step 4 verifies the *queue*, not delivery.
R44 Docs authorised for step 5's docs-only commit (supersedes R37's list): DATA_MODEL.md → v1.3 (`year_groups`, the FK changes, the `*_legacy` columns actually shipped, `displayName()`, `submitted_at`, `TutorStatusTransitions`); CHECKPOINTS.md (CP1/CP2 boxes done, CP1 box 7 payout clause still deferred to CP5, CP3 gains the booking-time permit check and the frozen `trialPrice()`); DECISIONS.md → ADR-004 year groups as a controlled list, ADR-005 trustutor-rehearsal per R41, ADR-006 brand TrusTutor per R40, ADR-007 mail deferred per R43; step-1 Low L3 (the homepage-only `ContentBlock` docblock); HOW-WE-WORK §14 gains R16, R18, R20 and R30 as lessons — four lines, the only HOW-WE-WORK edit ever authorised. PRD unchanged.
R45 The planner does owner-console work (owner, 2026-09-19). Forge, the Filament admin UI, GitHub settings and DNS are reachable by neither CC (no browser, no credentials — R20) nor by the owner's time budget, so they were falling to the owner by default. From now the planner performs them in the owner's browser and records each one as a numbered ruling here, so the change is visible to CC and the record stays true. Unchanged: the planner still writes no code and no repo file except PLAN.md and CLAUDE.local.md; CC still owns git, tests and STATUS. The planner tells the owner before any destructive or production-touching console action and never signs in on his behalf — if a console needs a login the planner stops and says so.
R46 Site name verified `TrusTutor` on the live server (planner, 2026-09-19, under R45): `https://rehearsal.trustutor.com/admin/login` returns `<title>Login - TrusTutor</title>`. The `trusTutor` casing STATUS 01:20 reported is resolved; step 4 does not halt on it. CC re-confirms cheaply with `curl -s https://rehearsal.trustutor.com/admin/login | grep -o '<title>[^<]*</title>'` rather than taking this on trust.
R47 The scheduler's last run is provable from the shell, so it is no longer an owner action: Forge writes each scheduled job's output under `/home/forge/.forge/`, and the file's mtime is the last run. `ls -l /home/forge/.forge/`, `tail -n 40` of that log and `crontab -l` are added to the read-only allow-list in CLAUDE.local.md. Likewise the four seed counts come from `php8.4 artisan db:show --counts` (also added), never raw SQL and never the admin UI. Step 4's two "unproven" items are therefore CC's to close, not the owner's.
R48 R46 was wrong and is withdrawn (planner, 2026-09-19). It verified the brand at `/admin/login`, whose title is Filament's panel brand from `APP_NAME`, not the `site_name` setting the public pages render — CC was right to refuse it. Corrected evidence: the owner set the setting and the PUBLIC homepage `https://rehearsal.trustutor.com/` now returns `<title>Welcome - TrusTutor</title>`, read in the browser at 2026-09-19. Step 4's last mismatch is closed; CC re-confirms with one `curl` on `/` (not `/admin/login`) and proceeds to step 5 in the same run without a further owner gate. Standing lesson for R45 console work: verify a setting on the surface that actually reads it, and name that surface in the ruling.
R49 Brand pack placed, not applied (owner, 2026-09-19). The designer's pack is in the repo at `docs/brand/` (24 files, 2.3 MB: `README.md` with the lockup rules, `theme/colors.css` + `theme/tokens.json` + `theme/tailwind.colors.js`, `lockup.html`, wordmark/symbol PNGs in five colourways, favicons, apple-touch, maskable, app icon, avatar, OG image). It is REFERENCE ONLY this cycle — CC does not restyle anything, does not import `colors.css`, does not swap a favicon and does not touch Tailwind config. Brand goes in at its proper checkpoints: the site logo, favicon and OG image are already admin-uploadable settings from CP1 (PRD §12), so they are owner uploads whenever wanted; the front-end restyle (palette, type, radii, lockups) belongs to a named branding step in the CP6 plan, and the Filament panel theme with it. Binding facts when that step comes: primary `#64011D`, accent `#FD4C53` (UI and 24px+ only — 3.4:1 on white, never body text), page ground `#FBF8F7`, admin sidebar `#240A12`; type Schibsted Grotesk for UI, Quicksand SemiBold for the tagline only, IBM Plex Mono for data; radii 10/16 web, 8/12 admin; tagline "Tutoring, Electrified."; the tagline never sits beside the wordmark below 768px and is dropped below 480px. Outstanding from the designer, tracked here and needed before production (CP8): a vector redraw — everything supplied is raster, so nothing may be scaled above the sizes given — the tagline outlined in the master, a heavier symbol cut for 16-32px, and SVG/CMYK/@2x/@3x per lockup and colourway. `docs/brand/` is committed to a PUBLIC repo (R5): it is the owner's own brand and carries no secret, but the mark is visible before launch — the owner accepted this.

## Steps
1. `cp/2.5a-rulings` — [done] PR #8, merged `a6281eb`. Do not redo.
2. `cp/2.5b-year-groups` — [done] PR #9, merged `530b5e5`. Do not redo.
3. `cp/2.5c-tutor-lifecycle` — [done] PR #10, merged `6214ad4`. Do not redo.
4. Verify trustutor-rehearsal — owner actions are complete (server, site, HTTPS, environment, Horizon process, scheduler job, first deploy `50d53ee`, seed run once, CLAUDE.local.md `## Servers` filled). Done means, using only the read-only allow-list and quoting no secrets: `php artisan about` shows env `rehearsal`, debug **false**, PHP 8.4, the `trustutor_rehearsal` connection; `php artisan horizon:status` reports `running` (if it does not, STOP — do not restart it, report it); the scheduler has ticked within the last two minutes (evidence from `storage/logs/laravel.log` or `schedule:list` — a bare `schedule:list` is read-only and allowed); migrations are all `Ran`; `curl -I` gives 200 on `/`, `/login`, `/admin/login`, `/tutors` and a valid certificate; the CP0 seed counts read 5 curricula / ~25 subjects / 9 price bands / 20 settings / 1 admin plus the pages, content blocks and 21 year-group rows; `site.name` is set to `TrusTutor` (this one setting change is a state-changing action — ask for the per-command "yes", or leave it to the owner in the admin UI and say so). Every command quoted with its output. Any mismatch halts the step and goes to STATUS §6 — nothing on that server is "just fixed" — halt: yes, on any mismatch; otherwise continue to step 5.
5. Close — done means: the docs-only commit per R44, pushed to `main` (which deploys nothing, per R41); STATUS rewritten with §2 "cycle 03 complete", §7 "next: CP3 plan — booking, trial, cancellation; opens with the R42 Lows pass; backend-dev review before merge on CP3/4/5 to be confirmed"; CYCLE-LOG END for cycle 03 with the final suite count and the advisor total — halt: yes.

## Out of scope this cycle
CP3 onward (booking, lessons, money); the R42 Lows themselves; payment-gateway and video-provider registries; D-09 mail provider; any production server; Arabic; browser automation; editing PRD.md or CLAUDE.md; any Forge or DNS change.

## Carried forward
CP3 plan: the R42 Lows pass first, then `LessonStateMachine`, `BookLesson` with booking-time `bookable()` and permit checks, trial price frozen from `trialPrice()` (unifying the onboarding preview's rounding), learner delete-guards, user-delete behaviour, overlapping-offer transactional check, `whereDate` in `bookable()` vs the new index, in-memory search pagination; backend-dev review before merge on CP3/4/5 (confirm). D-02 gateway and D-04 legal entity by the CP5 plan. **D-09 mail provider** (R43) — its own step, needed before any CP6 email is believable. **Branding step in the CP6 plan** (R49): apply `docs/brand/theme/colors.css` and the type/radii tokens to the front end and the Filament panel, wire the favicon/OG/apple-touch set, build the C and D lockups per `lockup.html` with the 768px and 480px rules; the vector redraw and the heavier small symbol cut must be in hand before CP8. `trustutor-production` at CP8, built from R41's recipe rather than improvised. Optional owner items, still open: branch protection on `main` (R7/R23); `.claude/settings.local.json` (ADR-002).
