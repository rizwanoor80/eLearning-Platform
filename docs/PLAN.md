# PLAN — cycle 01 r2 — 2026-09-16
Revision note: r2 replaces r1 after the owner's ruling to drop Docker (R4). r1 steps 1–2 are closed (CYCLE-LOG END 18:35) — do not redo them; step 1 is re-run only for the native checks listed below. Everything else is restated in full because a revision overwrites, never appends. Execute step 0 first, then from the first unfinished step.
Standing (applies to every step): suite green with the literal count before every push;
`git log origin/main..HEAD` quoted before every push; each step names its ADVISOR entries
or states none was triggered; temporary scripts deleted from every server and said so;
if the session nears its limit, STATUS.md first.
Advisor minimum this cycle: 4 consultations in total — #1 already logged (18:33); #2 native-Windows design before the first code edit (step 3); #3 auth boundary (step 4, mandatory); #4 before the PR (step 8).
Authorisation boundary: CC may make the docs-only commit in step 0 and push it to `main`; create local databases and roles on this machine (local only); create and push branch `cp/0-foundation`; open PR #1; run the fresh-subagent review; and — since `gh auth status` shows `rizwanoor80` — set the `main` branch protection in R7 via `gh api`. CC may NOT merge PR #1 (owner GO), run any installer that needs an administrator prompt (those are owner actions), sign up for or configure any external service other than GitHub, create real identities, send real email, touch Docker Desktop, or run anything outside the checkout and the local services named here.

## Rulings (owner decisions, numbered, carried across revisions)
R1 D-01 confirmed: Inertia + Vue 3. R2 D-03 confirmed: Daily.co (not touched this cycle). R3 D-06 confirmed: trial discount 50%.
R4 (replaces r1 R4) D-07: native Windows via Laravel Herd — no Docker, no Sail, no WSL. PHP 8.3 from Herd (8.4 only if Herd cannot provide 8.3 — then 8.4 everywhere incl. CI, DECISION logged, CLAUDE.md Stack line updated); Composer and Node from Herd; PostgreSQL 16 and a Redis-compatible server on localhost (Herd Pro services or native installs). Horizon is installed as a dependency so production config and CI are real, but `php artisan horizon` is never run on Windows: the local worker is `php artisan queue:work redis`. `composer install` must succeed on Windows with no manual flags (`config.platform` faking `ext-pcntl`/`ext-posix` plus `platform-check: php-only`, or an equivalent the advisor approves). Reverb is installed and configured; if `reverb:start` cannot boot on Windows, log a DECISION and defer its first run to CP7. Site served via `herd link project-elearning` → `http://project-elearning.test`; if Herd refuses the spaced path, `php artisan serve` is acceptable (DECISION). The checkout stays `C:\project elearning` — quote the path everywhere.
R5 D-08: public repo `https://github.com/rizwanoor80/eLearning-Platform.git`, default branch `main`. The no-secrets / no-real-identity rule is absolute: nothing private is ever committed.
R6 The initial push of `main` is done (`5179aff`, `accde12`). All code goes on `cp/0-foundation` → PR #1 → review → halt for the owner's merge GO. No merge without it.
R7 Branch protection on `main`: require a pull request, require the CI status check, block force-pushes and branch deletion; do NOT require code-owner approval and do NOT enforce for administrators (docs-only commits from this checkout must land on `main` directly per HOW-WE-WORK §8). `.github/CODEOWNERS` = `* @rizwanoor80`.
R8 Tests run against PostgreSQL 16 — local databases `elearning` (dev) and `elearning_testing` (tests), a `postgres:16` service container in CI — never SQLite; later checkpoints depend on Postgres partial unique indexes.
R9 Starter: the official Laravel 12 Vue starter kit (Inertia 2, Vue 3, Tailwind 4, Fortify-based auth) with Pest. Generated third-party UI components under `resources/js/components/ui/` may be excluded from the RTL grep only if they cannot be converted to logical utilities; any exclusion is a DECISION entry and is listed in STATUS.md §4. Every app-authored file is covered by the grep.
R10 Seeded admin credentials come from `.env` keys (`ADMIN_EMAIL`, `ADMIN_PASSWORD`) with placeholder values in `.env.example`; no real email or password anywhere in the repo.
R11 The kit's STATUS.md owner actions 1–5 are closed by R1–R5; D-02 stays open (Carried forward).
R12 Local service credentials live only in `CLAUDE.local.md` under `## Local services` (git-ignored, owner-written) and in `.env` (git-ignored). Read them, never quote them: no password, connection string or password-bearing URL in any log entry, STATUS.md, commit, report or PR.
R13 Docker Desktop is left alone — not uninstalled, not repaired, not referenced by any file in the repo.

## Steps
0. Record the ruling (docs-only, on `main`) — done means: one commit pushed to `main` containing: DECISIONS.md row `ADR-001 | 2026-09-16 | Local dev environment = Laravel Herd, native Windows, no Docker | Docker Desktop crash-loops on a stale inference socket (docker/desktop-feedback #460) and recovery needs manual steps CC cannot perform; native is also faster | Accepted (owner ruling)`; PRD §11 D-07 status cell → "Decided (owner, 2026-09-16): Herd native, no Docker — ADR-001"; CHECKPOINTS.md CP0 environment line → "Laravel 12 on Laravel Herd (native Windows, per D-07 / ADR-001): PostgreSQL 16, Redis, Horizon (installed, not run locally — `queue:work` in dev), Reverb, Inertia + Vue, Tailwind, Filament, Pest."; CLAUDE.md gains a `## Local environment` section of at most six lines condensing R4 and R12 — files: docs/DECISIONS.md, docs/PRD.md (that one cell only), docs/CHECKPOINTS.md (that one line only), CLAUDE.md — this plan explicitly authorises these four edits; commit hash quoted.
1. Environment check (r2, native) — done means: quoted output of `php -v` (8.3.x), `composer --version`, `node -v`, `npm -v`, Herd's version or binary path, `psql --version` or a successful connection to PostgreSQL 16 on localhost, and a Redis `PING` → `PONG` (`redis-cli` or `memurai-cli`) — halt: only if PostgreSQL 16 or Redis is missing or unreachable → Owner action with both routes, option 1 marked (Recommended): (1) Herd Pro → Services → add PostgreSQL 16 and Redis, start both — one click, managed by the same tool that runs PHP; (2) free: `winget install -e --id PostgreSQL.PostgreSQL.16` and `winget install -e --id Memurai.MemuraiDeveloper`. Either way the owner then adds a `## Local services` section to `CLAUDE.local.md` with Postgres host, port, superuser name and password, and Redis host and port, and replies `update`.
2. Repo bootstrap — [done in r1: `5179aff` "Day-one kit", `accde12` "PLAN.md cycle 01 r1", `CLAUDE.local.md` ignored]. Nothing to do; do not redo.
3. Scaffold on branch `cp/0-foundation` — done means: role `elearning` and databases `elearning`, `elearning_testing` created from the CLAUDE.local.md superuser (R12: never quote it); Laravel 12 created in a temporary directory and moved in without touching docs/, CLAUDE.md, CLAUDE.local.md*, .gitignore entries; `.env` on pgsql + redis; `php artisan migrate` green on Postgres; Horizon and Reverb installed and configured per R4; Filament installed with a panel at `/admin`; a fresh `composer install` on Windows succeeds with no flags (quoted); `composer test` green with the count; `npm run build` green; site answers HTTP 200 per R4 (quoted) — ADVISOR #2 before the first edit: composer platform config, Horizon and Reverb on Windows, scaffolding into a non-empty folder, RTL-grep strategy, Postgres-only test config.
4. Roles and auth — done means: `users.role` enum (`account_owner|tutor|admin`) via `App\Enums\Role`; registration + login + email verification for `account_owner` at `/register` landing on an empty `/dashboard`, and for `tutor` at `/tutor/register` landing on a `/tutor/onboarding` placeholder page; no admin registration route — admin exists only via seeder and logs into Filament; Policies gate the parent area, tutor area and the Filament panel by role; feature tests for each entry point, each landing page and each cross-role denial; suite count stated — files: app/Enums, app/Models/User, app/Policies, routes, resources/js/pages — ADVISOR #3 (auth boundary, mandatory).
5. Money + Settings — done means: `App\Support\Money` (integer fils; add, subtract, percentage, format "AED 120.00", no float leakage) with unit tests; `settings` table + `Settings` facade with cache; unit tests for read, write and cache-bust; suite count stated — files: app/Support, app/Services/Settings, database/migrations, tests/Unit.
6. Seeders — done means: curricula (GCSE, A_LEVEL, IB_MYP, IB_DP, CBSE), ≈25 subjects, price bands per curriculum × tier from PRD §3 (only the tiers that exist for that curriculum), settings from DATA_MODEL.md `settings`, admin user per R10; factories for every model created so far; `php artisan migrate:fresh --seed` green on the local `elearning` database only; row counts quoted — files: database/seeders, database/factories.
7. Layout shell + RTL guard — done means: the app layout sets `<html dir>` from the user's locale (always `ltr` in v1) and uses logical utilities only; `scripts/rtl-check.sh` greps `resources/` for `ml-|mr-|pl-|pr-|left-|right-|text-left|text-right` (R9 exclusion rule) and is wired into `composer test` (must run under Git Bash on Windows and on Linux); a VERIFICATION entry quotes it failing on a temporary component using `ml-4` and passing after `ms-4` (temporary component removed and said so) — files: resources/js/layouts, scripts/, composer.json.
8. CI + branch protection + PR — done means: `.github/workflows/ci.yml` runs Pint (`--test`), `composer test` against `postgres:16` + `redis` services, `npm run build`, the RTL check, and one command proving Horizon boots on Linux (e.g. `php artisan horizon:status` against the Redis service) on every PR; `.github/CODEOWNERS` per R7; branch protection per R7 via `gh api` (settings quoted); suite green with the literal count; `git log origin/main..HEAD` quoted; branch pushed; PR #1 opened with the CP0 acceptance list from CHECKPOINTS.md as its checklist; CI green on the PR (run URL quoted) — ADVISOR #4 before opening the PR.
9. Review + halt — done means: fresh-subagent adversarial review of PR #1 logged as REVIEW with numbered verdicts (Low doc-only findings fixed in-cycle with a DEVIATION entry; anything else stops the cycle); STATUS.md rewritten with §7 "Owner action 1: GO to merge PR #1 — reply `update` when done" — halt: yes, owner GO required.

## Out of scope this cycle
Docker in any form; anything from CP1 onward; the tutor onboarding wizard beyond its placeholder page; any external service other than GitHub (no Daily, Stripe, email provider); hosting or a rehearsal server; Arabic translation; any DECISIONS.md row other than ADR-001.

## Carried forward
D-02 gateway and D-04 legal entity: settle by the CP5 plan (backend dev verifies saved-card charging and entity eligibility). D-05 brand and domain: by CP6. Human review gating (backend dev before merge on CP3/4/5, UI dev after merge) to be confirmed in the CP3 plan. Reference spec pack not yet in docs/reference/ — add when convenient; the PRD wins regardless. Rehearsal server (Linux) is where Horizon first runs for real — plan it before CP5.
