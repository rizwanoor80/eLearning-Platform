# PLAN — cycle 02 r4 — 2026-09-18
Revision note: r4 = r3 plus the owner's authorisation of fix-loop round 1 on PR #4 (R28), a seeder rule (R29) and a re-entry design rule for the remaining sub-cycles (R30). Sub-cycle 1b is closed (`10e6198`). r3 = r2 plus the owner's authorisation of fix-loop round 2 on PR #3 (R27). r2 = r1 plus the owner's ruling on the 1b rate-band finding (R26) and one added 1c line. Step 0 (`646ce06`) and sub-cycle 1a (`c08bad8`) are closed — do not redo. Sub-cycle 1b resumes on its open PR #3 per R26: fix on the existing branch, re-run the suite, push, re-request a fresh review, then merge under R21 and continue to 1c. Everything else is restated in full because a revision overwrites, never appends. Rulings R1–R18 from cycle 01 carry unchanged unless restated here.
Standing (applies to every step): suite green with the literal count before every push;
`git log origin/main..HEAD` quoted before every push; each step names its ADVISOR entries
or states none was triggered; temporary scripts deleted from every server and said so;
if the session nears its limit, STATUS.md first. Non-interactive flags on every command;
ten-minute stall rule (R16). Herd and its services are owner-operated (R18). Do not stop
between steps or sub-cycles — the only halts are those named below, a stop condition, or
the session limit. Verification is tests + `curl`/HTTP checks only; no browser automation
(R20). STATUS.md is rewritten at every step boundary, push, merge and halt, with §8 kept current.
Advisor minimum: 2 per sub-cycle (design before the first edit; before opening the PR), plus the mandatory triggers (auth boundary, money, frozen files, plan–repo conflict). Consultations are logged as their own ADVISOR entries, never folded into other entries.
Authorisation boundary: CC may merge PR #1 (R19); write the post-merge record and every docs-only commit to `main`; create and push `cp/1a-…` … `cp/2c-…` branches; open PRs; run fresh-subagent reviews; merge a programme PR only under the merge rule in R21; set or read branch protection via `gh api` (read is enough — R23). CC may NOT merge outside the merge rule, create `.claude/settings.local.json`, sign up for or configure any external service, create real identities, send real email, run installers, touch Docker or Herd, or use any browser tool.

## Rulings (owner decisions, numbered, carried across revisions)
R19 GO to merge PR #1 (owner, 2026-09-17, after reviewing the REVIEW entry and CI). Merge with a merge commit, not squash, so the cycle-01 history stays readable. `cp/0-foundation` is left in place (CLAUDE.md forbids deleting branches other than a merged `hold/<step>`).
R20 No browser automation in this cycle or any programme: the desktop app's browser prompts per action on local sites and cannot be granted a standing permission, so an unattended run would stall. Rendered-page checks are `curl` for status codes and HTML content; behaviour is proven by feature tests. A visual pass, if wanted, happens in a hand-run cycle.
R21 Programme "CP1–CP2" is authorised (§12). Sub-cycles run in order, each: branch `cp/<id>-<slug>` from current `main` → PR with the sub-cycle's acceptance boxes as its checklist → fresh-subagent adversarial review → fix loop (cap 2) → merge under the rule → post-merge record on `main` → read-only smoke (`composer test` on `main`, count quoted; `curl` on `/`, `/login`, `/admin/login` → expected status codes) → next sub-cycle. **Merge rule:** review has no open Medium or High; CI green on the PR head; diff touches only the areas the step names plus tests, migrations, seeders, factories, routes and resources; no change to CLAUDE.md, docs/PRD.md, docs/DATA_MODEL.md, docs/CHECKPOINTS.md, docs/HOW-WE-WORK.md or `.github/workflows/ci.yml` except where a step names it; no frozen file exists yet. Migrations are allowed (no remote environment exists; local only). Any Medium+ finding or rule miss → PR stays open, STATUS §7 names it "awaiting owner", programme halts (sub-cycles are dependent, so there is no "next independent item"). **Stop conditions:** smoke failure; a regression the fix loop cannot clear in two rounds; any High security finding; a local service unreachable (R18 halt); the owner typing `stop`; the session limit (STATUS first, then stop — the next `update` resumes from the §8 board). **Resume cap:** at most 8 automatic continuations across the programme; then halt for the 24-hour owner review even if sub-cycles remain. **Board:** STATUS §8 lists the six sub-cycles with state, branch, PR, review verdict, merge hash.
R22 Human reviews this programme: none before merge (CP1/CP2 are not the backend dev's CP3–CP5). STATUS §8 lists every merged PR so the UI dev can review the front-end after the fact; findings come back through the next plan.
R23 Branch protection on `main` is an owner action (STATUS 19:08 owner action 2). CC reads it once at the start (`gh api repos/rizwanoor80/eLearning-Platform/branches/main/protection`, read-only) and records set / not set in STATUS §1; either way the programme proceeds — the protocol, not GitHub, is the gate.
R24 Cycle-01 review note #6 (no test for `scripts/rtl-check.sh`'s own logic) is closed in sub-cycle 1a: a Pest test that runs the script against two fixture files (one physical, one logical utility) and asserts the exit codes.
R26 Rate-band validation (owner, 2026-09-18, closing PR #3's Medium finding): the tutor's single `hourly_rate` must fall inside the band of **every** curriculum they teach at their highest tier — the intersection, not the union. If the intersection is empty, the rate step rejects with a message naming the curricula whose bands do not overlap; nothing is silently picked. No schema change: `hourly_rate` stays on `tutor_profiles` per DATA_MODEL v1.2. Tests: two curricula with identical bands (passes as before); two with different overlapping bands (rate inside both passes, rate inside only one is rejected naming the other); two with non-overlapping bands (rejected naming both). Counts as the fix loop's first round; the re-review is a fresh subagent with no prior context.
R27 Fix-loop round 2 of 2 on PR #3 (owner, 2026-09-18), closing the second review's Medium and its Low together: (a) `storeSubjects()`, after replacing the subject list, recomputes the band for the new list; if `hourly_rate` is set and no longer fits — or the new band is conflicting or missing — `hourly_rate` is cleared to null so `currentStep()` returns the tutor to `rate` with the correct band shown; (b) `complete()` re-validates the stored rate against the current band and refuses (409) if out of range; (c) a curriculum at the highest tier with no current `price_bands` row is a conflict named in the rejection message, never silently dropped (R26's "nothing is silently picked"). Tests: subjects swap to a narrower band clears the rate and returns step `rate`; a swap that still fits keeps the rate; completion with a since-invalidated rate is refused; a curriculum with no current band is named as a conflict. Also correct PR #3's description to the current test count in the same push. Re-review by a fresh subagent. If that review returns any Medium or High, R21's stop condition applies: halt the whole programme for the 24-hour owner review — no round 3.
R28 Fix-loop round 1 of 2 on PR #4 (owner, 2026-09-18): (a) `currentStep()` counts only `pending` and `accepted` documents as uploaded — a `rejected` one returns a `changes_requested` tutor to the document step for that type, where `storeDocument` soft-deletes the old row and creates a new pending one; `complete()` stays blocked until replaced and `ApproveTutor` until accepted; draft flow unchanged. Tests: rejected required document + `changes_requested` → step `document` for that type; upload → new pending row, old soft-deleted, step advances; `complete()` → `pending_review`; approve blocked until accepted, then allowed; a `pending` document still counts as uploaded. (b) `SerializesModels` on the six tutor events. (c) `status === Active` added to the `access-admin-area` gate and to `TutorDocumentPolicy`'s admin branch, with a test that a disabled admin is refused. Re-review by a fresh subagent; a further Medium is round 2 (last); a Medium after that halts the programme.
R29 `SettingsSeeder` never overwrites an existing row: insert missing keys only (`firstOrCreate` on `key`), with a test that a changed `site_name` survives a re-seed. Apply in the same round as R28 (it is 1c's editor that makes this matter).
R30 Re-entry rule for every remaining sub-cycle: any step, form or action that can be revisited after a later step has used its data must state, in the design ADVISOR entry, what downstream state it invalidates and how that state is re-derived or cleared, with a test per dependency. The design consult is not complete without this list. (Three misses of this class so far: rate after subjects, twice; documents after rejection.)
R25 Data placed by the planner is read-only for CC: PRD v1.2 §12, DATA_MODEL v1.2 and CHECKPOINTS v1.2 define every table, key and acceptance box below; where this plan and those files differ, the files win and CC discloses the difference in §6.

## Steps
0. Merge PR #1 and close cycle 01 — done means: `gh pr merge 1 --merge --no-interaction` quoted; `main` fast-forwarded locally; post-merge record committed on `main` (STATUS with §2 "cycle 01 complete", CYCLE-LOG END for cycle 01 with the final count and advisor summary); smoke on `main`: `composer test` green (count quoted), `curl` `/` → 200, `/login` → 200, `/admin/login` → 200, `/dashboard` unauthenticated → 302; branch protection read per R23 — halt: no.
1. Sub-cycle 1a `cp/1a-onboarding-core` — done means: `document_types` table + seeder (permit scan, ID/passport, qualifications, police clearance) + `tutor_documents` with `document_type_id`; `TutorProfile` model, statuses enum, `bookable()` scope, permit number/expiry, encrypted bank fields (`encrypted` casts, masked accessor showing last four of the IBAN); onboarding wizard steps 1–3 (personal, permit, one upload step per active document type on the private disk with signed URLs) resumable; R24's rtl-check test; feature tests for signed-URL expiry and direct-path 403 — closes CHECKPOINTS CP1 boxes 3 and 7.
2. Sub-cycle 1b `cp/1b-onboarding-complete` — [R26 applied (`8e0afcf`); second review found one Medium + one Low; resume here with round 2] done means: R27 applied on the existing branch with its tests; suite green with the count; pushed; fresh-subagent re-review logged as REVIEW; merged under R21 (fix loop round 2 of 2 — last); post-merge record; smoke. Original done-criteria for reference: bank-details step; subjects step (curriculum × subject × level range → `tutor_subjects`, `level_tier` derived via `CurriculumCode::tiers()`); rate step band-validated against the highest tier with the derived trial price shown; bio/headline/video; availability (weekly template + exceptions); agreement step recording `agreement_version` from a seeded placeholder `tutor_agreement` page row version 1 (the pages editor itself is 2c); completion sets `pending_review`; tests for band rejection, `bookable()` excluding expired permits, completion blocked without agreement — closes CP1 boxes 1, 2, 5.
3. Sub-cycle 1c `cp/1c-admin-approval` — [built, PR #4 open, halted on one Medium + one Low; resume here with round 1] done means: R28 and R29 applied on the existing branch with their tests; suite green with the count; pushed; fresh-subagent re-review logged as REVIEW; merged under R21; post-merge record; smoke. Original done-criteria for reference: Filament tutor approval queue with document viewer, per-document accept/reject, "request changes" note, approve/reject/suspend actions writing `audit_logs`; approval blocked while a required document type lacks an accepted document; Filament CRUD for document types; Filament settings editor with platform/site/mail/features tabs, logo and favicon uploads, `head_scripts` admin-only; site name, sender name/address and footer read from settings in layout and mail; admin-users resource (create/disable, audited, no self-registration route); permit expiry job (30d, 7d warnings, auto-hide) with scheduled-task tests; the CP1 emails; tests for the approve audit row, the document-type add/remove behaviour, the site-name change without deploy, admin-users access; the price-bands editor warns (non-blocking notice, tested) when two curricula's bands at the same tier do not overlap, so the R26 data error is caught at the source — closes CP1 boxes 4, 6, 8, 9. All CP1 boxes closed; STATUS §8 marks CP1 done.
4. Sub-cycle 2a `cp/2a-learners-slots` — done means: learner CRUD under the account owner (adult students get a self-learner at registration); `SlotCalculator` service with the full unit matrix in CHECKPOINTS CP2 box 2 (blocked/extra exceptions, booked lessons stubbed via a minimal `lessons` table with only the columns SlotCalculator needs — the full lesson schema is CP3, and the migration is written so CP3 extends it rather than replaces it, DECISION logged; active recurring slots stubbed the same way; `booking_min_lead_hours`, `booking_max_days`) — closes CP2 box 2.
5. Sub-cycle 2b `cp/2b-search-profile` — done means: search page with filters and sorts per CHECKPOINTS CP2, Postgres query with indexes, only `bookable()` tutors with ≥1 slot in 14 days; tutor public profile page with rate, trial price, next slots; `features.reviews` and `features.messaging` gates in place as route/UI switches (features themselves are CP7); tests with suspended / expired-permit / no-availability fixtures — closes CP2 box 1 and the toggle half of box 3.
6. Sub-cycle 2c `cp/2c-match-pages-content` — done means: match request form → Filament queue → admin picks 1–3 tutors → suggestions email containing only bookable tutors; hidden when `features.match_requests` is off (404 on the route, entry points absent); `pages` + `page_versions` editor with Publish, public routes for terms/privacy/tutor-agreement/safeguarding/about/contact rendered from the current version, footer links, seeded "DRAFT — replace before launch" bodies; the 1b placeholder agreement row migrated into this structure keeping version 1; `content_blocks` editor and homepage rendering; tests per CP2 boxes 3–6 — closes all CP2 boxes; STATUS §8 marks CP2 done.
7. Programme end — done means: STATUS.md with §8 complete (six rows, hashes, PR links, verdicts), §2 "programme CP1–CP2 complete", §7 "Owner action 1: 24-hour review — reply `update` to acknowledge, or `where are we?` to the planner" — halt: yes (the §12 review point).

## Out of scope this cycle
CP3 onward; payment-gateway and video-provider registries (CP5/CP6); any external service (no Daily, Stripe, Forge, email provider); hosting; Arabic; browser automation; editing the v1.2 docs or CLAUDE.md except through a ruling here (none this cycle).

## Carried forward
Forge rehearsal server as its own hand-run cycle after this programme (Linux, PHP 8.4, PostgreSQL 18, Horizon for real). Branch protection and `.claude/settings.local.json`: owner actions, optional. D-02 gateway and D-04 entity by the CP5 plan; D-05 brand is a site setting from 1c, domain and sender by CP6. Backend dev review before merge on CP3/4/5 — confirm in the CP3 plan. Programme-end review list (not blocking, from 1c's review): Fortify verification/reset mails still use `config('mail.from')` rather than the settings sender; approval queue "Submitted" column sorts on profile creation, not submission; last-active-admin check not transactional; `review_note` not cleared on resubmit; double queue hop on tutor mails; logo/favicon uploads untested and not rendered; N+1 on the queue's documents column; duplicate price-band row 500s. HOW-WE-WORK §14 lessons to add at the next "refresh the brief": non-interactive flags (R16), Herd owner-operated (R18), no browser automation in programmes (R20), pre-PR advisor consultation gets its own entry.
