# PLAN — cycle 01 r1 — 2026-09-16
Standing (applies to every step): suite green with the literal count before every push;
`git log origin/main..HEAD` quoted before every push; each step names its ADVISOR entries
or states none was triggered; temporary scripts deleted from every server and said so;
if the session nears its limit, STATUS.md first.
Advisor minimum this cycle: 3 consultations (step 2: design before the first edit; step 4: auth boundary, mandatory; step 8: before the PR).
Authorisation boundary: CC may `git init`, make the initial docs-only commits on `main` and push them to `origin` (R6), create and push branch `cp/0-foundation`, open PR #1, run the fresh-subagent review, and — only if `gh auth status` already shows an authenticated account — set the `main` branch protection in R7 via `gh api`. CC may NOT merge PR #1 (owner GO), sign up for or configure any external service other than GitHub, create real identities, send real email, or run anything outside the checkout and its Docker containers.

## Rulings (owner decisions, numbered, carried across revisions)
R1 D-01 confirmed: Inertia + Vue 3. R2 D-03 confirmed: Daily.co (not touched this cycle). R3 D-06 confirmed: trial discount 50%.
R4 D-07: Laravel Sail on Docker Desktop (WSL2 backend). The checkout stays at `C:\project elearning` — the path has a space; quote it everywhere. Every PHP, composer, npm and artisan command runs inside the Sail `laravel.test` container; no native-Windows PHP path. From Git Bash prefer `docker compose exec -T laravel.test …` over the `sail` wrapper if the wrapper fails without a TTY (log a DECISION).
R5 D-08: public repo `https://github.com/rizwanoor80/eLearning-Platform.git`, default branch `main`. The no-secrets / no-real-identity rule is absolute: nothing private is ever committed.
R6 The initial push of `main` (day-one kit + this plan, docs only) is authorised in advance. All code goes on `cp/0-foundation` → PR #1 → review → halt for the owner's merge GO. No merge without it.
R7 Branch protection on `main`: require a pull request, require the CI status check, block force-pushes and branch deletion; do NOT require code-owner approval and do NOT enforce for administrators (docs-only commits from this checkout must land on `main` directly per HOW-WE-WORK §8). `.github/CODEOWNERS` = `* @rizwanoor80`.
R8 Tests run against PostgreSQL 16 (a `testing` database on the Sail `pgsql` service locally; a `postgres:16` service container in CI), never SQLite — later checkpoints depend on Postgres partial unique indexes.
R9 Starter: the official Laravel 12 Vue starter kit (Inertia 2, Vue 3, Tailwind 4, Fortify-based auth) with Pest. Generated third-party UI components under `resources/js/components/ui/` may be excluded from the RTL grep only if they cannot be converted to logical utilities; any exclusion is a DECISION entry and is listed in STATUS.md §4. Every app-authored file is covered by the grep.
R10 Seeded admin credentials come from `.env` keys (`ADMIN_EMAIL`, `ADMIN_PASSWORD`) with placeholder values in `.env.example`; no real email or password anywhere in the repo.
R11 R1–R5 close the kit's STATUS.md owner actions 1–5. D-02 stays open (see Carried forward). Stale text in the kit's STATUS.md is superseded by the first STATUS.md CC writes.

## Steps
1. Environment check — done means: `docker --version`, `docker compose version` and `wsl -l -v` output quoted in a VERIFICATION entry showing Docker Desktop with a WSL2 distro — files: none — halt: only if Docker Desktop or WSL2 is missing → Owner action with the exact install steps (Docker Desktop, WSL2 backend, reboot), "reply `update` when done". There is no git repository yet (kit STATUS.md §1): skip fetch/fast-forward until step 2.
2. Repo bootstrap — done means: `git init` with branch `main`; `.gitignore` containing `CLAUDE.local.md`; commit "Day-one kit" (CLAUDE.md, CLAUDE.local.md.example, .gitignore, docs/*); commit "PLAN.md cycle 01 r1"; `origin` added per R5; `git push -u origin main` succeeds; both hashes quoted; `CLAUDE.local.md` copied from the example and shown as ignored by `git status --ignored` — files: .gitignore, docs — ADVISOR #1 before the first edit: scaffolding into a non-empty folder, starter-kit choice, RTL-grep strategy, Postgres-only tests.
3. Scaffold on branch `cp/0-foundation` — done means: Laravel 12 created in a temporary directory and moved in without touching docs/, CLAUDE.md, CLAUDE.local.md*, .gitignore entries; Sail installed with pgsql, redis, mailpit; `docker compose up -d` healthy (output quoted); PHP 8.3; `php artisan migrate` green on Postgres; Horizon and Reverb installed and configured (queue connection `redis`; no jobs yet); Filament installed with a panel at `/admin`; `composer test` green with the count; `npm run build` green — files: whole app skeleton.
4. Roles and auth — done means: `users.role` enum (`account_owner|tutor|admin`) via `App\Enums\Role`; registration + login + email verification for `account_owner` at `/register` landing on an empty `/dashboard`, and for `tutor` at `/tutor/register` landing on a `/tutor/onboarding` placeholder page; no admin registration route — admin exists only via seeder and logs into Filament; Policies gate the parent area, tutor area and the Filament panel by role; feature tests for each entry point, each landing page and each cross-role denial; suite count stated — files: app/Enums, app/Models/User, app/Policies, routes, resources/js/pages — ADVISOR #2 (auth boundary, mandatory).
5. Money + Settings — done means: `App\Support\Money` (integer fils; add, subtract, percentage, format "AED 120.00", no float leakage) with unit tests; `settings` table + `Settings` facade with cache; unit tests for read, write and cache-bust; suite count stated — files: app/Support, app/Services/Settings, database/migrations, tests/Unit.
6. Seeders — done means: curricula (GCSE, A_LEVEL, IB_MYP, IB_DP, CBSE), ≈25 subjects, price bands per curriculum × tier from PRD §3 (only the tiers that exist for that curriculum), settings from DATA_MODEL.md `settings`, admin user per R10; factories for every model created so far; `php artisan migrate:fresh --seed` green locally (local only, never elsewhere); row counts quoted — files: database/seeders, database/factories.
7. Layout shell + RTL guard — done means: the app layout sets `<html dir>` from the user's locale (always `ltr` in v1) and uses logical utilities only; `scripts/rtl-check.sh` greps `resources/` for `ml-|mr-|pl-|pr-|left-|right-|text-left|text-right` (R9 exclusion rule) and is wired into `composer test`; a VERIFICATION entry quotes it failing on a temporary component using `ml-4` and passing after `ms-4` (temporary component removed and said so) — files: resources/js/layouts, scripts/, composer.json.
8. CI + branch protection + PR — done means: `.github/workflows/ci.yml` runs Pint (`--test`), `composer test` against `postgres:16` + `redis` services, `npm run build` and the RTL check on every PR; `.github/CODEOWNERS` per R7; branch protection per R7 via `gh api` if authenticated, otherwise an Owner action listing the exact settings to click; suite green with the literal count; `git log origin/main..HEAD` quoted; branch pushed; PR #1 opened with the CP0 acceptance list from CHECKPOINTS.md as its checklist; CI green on the PR (run URL quoted) — ADVISOR #3 before opening the PR.
9. Review + halt — done means: fresh-subagent adversarial review of PR #1 logged as REVIEW with numbered verdicts (Low doc-only findings fixed in-cycle with a DEVIATION entry; anything else stops the cycle); STATUS.md rewritten with §7 "Owner action 1: GO to merge PR #1 — reply `update` when done" — halt: yes, owner GO required.

## Out of scope this cycle
Anything from CP1 onward; the tutor onboarding wizard beyond its placeholder page; any external service other than GitHub (no Daily, Stripe, email provider); hosting or a rehearsal server; Arabic translation; DECISIONS.md rows (proposals go in STATUS.md §4; a later plan authorises writing them).

## Carried forward
D-02 gateway and D-04 legal entity: settle by the CP5 plan (backend dev verifies saved-card charging and entity eligibility). D-05 brand and domain: by CP6. Human review gating (backend dev before merge on CP3/4/5, UI dev after merge) to be confirmed in the CP3 plan. Reference spec pack not yet in docs/reference/ — add when convenient; the PRD wins regardless.
