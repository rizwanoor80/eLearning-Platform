# PLAN — cycle 01 r6 — 2026-09-17
Revision note: r6 = r5 plus one standing rule (no voluntary stops) and one closed-state update. Closed and not to be redone: r3 steps 0, 1, 3; r1 step 2; r4 step 0; r5 steps 0b, 4, 5, 6 (STATUS 16:35, branch `e97ba3e`, 89 tests). Execute from step 7. PostgreSQL and Redis are now Herd-managed again (owner action 2 closed, 2026-09-17 ~17:30) — verify with the usual port checks and drop the §6 carry-over. Everything else is restated in full because a revision overwrites, never appends.
Standing (applies to every step): suite green with the literal count before every push;
`git log origin/main..HEAD` quoted before every push; each step names its ADVISOR entries
or states none was triggered; temporary scripts deleted from every server and said so;
if the session nears its limit, STATUS.md first. Every artisan, composer, npm/npx and gh
command carries its non-interactive flag; any single command still running after ten
minutes is a stall — stop it, log a BLOCKER, never wait on it (R16). **Do not stop between
steps.** Halts exist only where the plan names one (step 9), where a blocker or R18 service
outage forces one, or where the session limit intervenes; "a sensible stopping point" is not a
halt condition. STATUS.md is still rewritten at every step boundary (§6 of HOW-WE-WORK) — but
the run continues after writing it.
Advisor minimum this cycle: 4 in total — already met (#1 18:33, #2 00:23, plan–repo conflict 01:45, and one more per STATUS 11:30). The two mandatory ones still apply regardless of count: before the first edit of step 4 (auth boundary) and before opening the PR in step 8.
Authorisation boundary: CC may make the docs-only commit in step 0 and push it to `main`; commit and push to `cp/0-foundation`; open PR #1; run the fresh-subagent review; and — `gh auth status` shows `rizwanoor80` — set the `main` branch protection in R7 via `gh api`. CC may NOT merge PR #1 (owner GO), attempt to create `.claude/settings.local.json` (guardrail — if it is absent, proceed under interactive approval and say so in STATUS §1), start, stop, restart or relaunch Herd or any Herd-managed service (R18), run any installer that needs an administrator prompt, sign up for or configure any external service other than GitHub, create real identities, send real email, touch Docker Desktop, or run anything outside the checkout and the local services named in CLAUDE.local.md.

## Rulings (owner decisions, numbered, carried across revisions)
R1 D-01 confirmed: Inertia + Vue 3. R2 D-03 confirmed: Daily.co (not touched this cycle). R3 D-06 confirmed: trial discount 50%.
R4 D-07 (final): native Windows via Laravel Herd Pro — no Docker, no Sail, no WSL. PHP 8.4 everywhere. Services from Herd, auto-started, details in CLAUDE.local.md `## Local services`: PostgreSQL 18 on 5432, Redis 7 on 6379, mail catcher on 2525. Horizon installed, never run on Windows (`queue:work redis` locally). `composer install` succeeds on Windows with no manual flags (done: `config.platform` + `platform-check: php-only`). Reverb installed; first run deferred to CP7 if it cannot boot on Windows. Site via `herd link project-elearning`. Checkout `C:\project elearning` — quote the path.
R5 D-08: public repo `https://github.com/rizwanoor80/eLearning-Platform.git`, branch `main`. No secrets, no real identities, ever.
R6 `main` holds `5179aff` … `804ea87`; code is on `cp/0-foundation` (`582cf62`, pushed) → PR #1 → review → halt for the owner's merge GO. No merge without it.
R7 Branch protection on `main`: require a pull request, require the CI status check, block force-pushes and branch deletion; do NOT require code-owner approval and do NOT enforce for administrators. `.github/CODEOWNERS` = `* @rizwanoor80`.
R8 Tests run against PostgreSQL 18 (`elearning_testing` locally — done; `postgres:18` service in CI), never SQLite.
R9 Starter: the official Laravel Vue starter kit as installed (Inertia 3, Vue 3, Tailwind 4, Fortify auth, Pest). Generated components under `resources/js/components/ui/` may be excluded from the RTL grep only if they cannot be converted; any exclusion is a DECISION and listed in STATUS §4. Every app-authored file is grep-covered.
R10 Seeded admin credentials from `.env` keys (`ADMIN_EMAIL`, `ADMIN_PASSWORD`) with placeholders in `.env.example`.
R11 Kit STATUS owner actions 1–5 closed. D-02 open (Carried forward).
R12 Local service credentials live only in `CLAUDE.local.md` and `.env`; never quote a password or connection string anywhere.
R13 Docker Desktop is left alone. R14 The permission allow-list is recorded as ADR-002; installing it is the owner's action, not CC's.
R15 (new) Framework: **Laravel 13 and Inertia 3**, as the official installer produced them on 2026-09-17 and CC kept after the advisor check (CYCLE-LOG 01:45). The kit's "Laravel 12 / Inertia 2" is superseded everywhere; no downgrade. Recorded as ADR-003.
R16 (new) Command hygiene: every artisan, composer, npm/npx and gh invocation carries its non-interactive flag (`--no-interaction` / `-n`, `--yes`, `--no-input`); a command still running after ten minutes is treated as a stall — stopped, logged as BLOCKER with the command, then worked around (never waited on). Recorded in CLAUDE.md (step 0) and as a HOW-WE-WORK §14 lesson at the next brief refresh.
R17 (new) PRD v1.2, DATA_MODEL v1.2, CHECKPOINTS v1.2 and the refreshed PROJECT_BRIEF are the owner-approved configurability changes (PRD §12). The planner has placed the four files in `docs/` uncommitted, already corrected to Laravel 13. CC commits them unchanged in step 0; CC does not edit them. CP0's scope is unchanged by v1.2 — the new items start at CP1.
R18 (new) Herd and its services are owner-operated. CC never launches, quits, restarts or "cleanly relaunches" Herd, and never starts PostgreSQL, Redis or the mail catcher itself — not via `herd`, not by running their binaries, not via Task Manager equivalents. Launching Herd from CC's shell ties Herd's console to that shell and breaks it when the shell ends (the EPIPE error boxes of 2026-09-17); services started from CC's shell die with the session. If any local service is unreachable, CC halts at once with a STATUS §7 owner action — "open Herd → Services and press Start on <service>; reply `update` when green" — and does not attempt a workaround. Any postgres/redis process CC itself started earlier today is to be noted in STATUS §6 so the owner can restart those services from Herd.

## Steps
0. Record the rulings (docs-only, on `main`) — done means: one commit pushed to `main` containing: the four planner-placed v1.2 files exactly as found on disk (docs/PRD.md, docs/DATA_MODEL.md, docs/CHECKPOINTS.md, docs/PROJECT_BRIEF.md — quote `git diff --stat` for them, do not modify); DECISIONS.md row `ADR-003 | 2026-09-17 | Framework pinned to Laravel 13 / Inertia 3 as installed | Official installer default on install day; Filament 5 accepts it; Laravel 12 is security-only; nothing in the design depends on the major | Accepted (owner ruling)`; CLAUDE.md Stack line → `Laravel 13 · PHP 8.4 · PostgreSQL 18 · Redis · Horizon · Reverb · Inertia 3 + Vue 3 (D-01) · Tailwind 4 · Filament 5 · Pest`; CLAUDE.md `## Commands` gains two lines: "- Every artisan, composer, npm/npx and gh command runs with its non-interactive flag (`--no-interaction`/`-n`, `--yes`, `--no-input`). Some `*:install` commands prompt by default — never assume." and "- A single command still running after 10 minutes is a stall: stop it, log a BLOCKER naming the command, work around it. Never wait on it."; CLAUDE.md invariant 16 appended: "16. **Integration credentials and provider choice live in the admin registries** (`payment_gateways`, `video_providers` — PRD §12): encrypted at rest, masked in the UI, never logged, never in the repo; drivers are resolved from the active row, never hard-wired." — files: docs/DECISIONS.md, CLAUDE.md, plus the four placed docs — commit hash quoted; then `git switch cp/0-foundation` and continue.
0b. Herd rule (docs-only, on `main`) — done means: CLAUDE.md `## Local environment` gains one line: "Herd and its services (PostgreSQL, Redis, mail) are started and stopped only by the owner in the Herd app. Never launch, restart or 'clean-relaunch' Herd or a service from this shell; if a service is down, halt with an Owner action."; committed and pushed to `main` with STATUS/CYCLE-LOG at the next docs-only commit — files: CLAUDE.md. May be batched with the next STATUS commit rather than done first.
1. Environment check — [done in r3, 00:27]. Do not redo. If PostgreSQL or Redis is unreachable at any point, apply R18 (halt, owner action) — no restarts from the shell.
2. Repo bootstrap — [done in r1]. Do not redo.
3. Scaffold — [done in r3, `582cf62`]. Do not redo. If `.claude/settings.local.json` exists at session start, note it in STATUS §1 (rules loaded); if not, note that too and proceed.
4. Roles and auth — [done in r5: `a38827c`, `686f818`, `c2bcc79`]. Do not redo. Original done-criteria for reference: `users.role` enum (`account_owner|tutor|admin`) via `App\Enums\Role`; registration + login + email verification for `account_owner` at `/register` landing on an empty `/dashboard`, and for `tutor` at `/tutor/register` landing on a `/tutor/onboarding` placeholder page; no admin registration route — admin exists only via seeder and logs into Filament; Policies gate the parent area, tutor area and the Filament panel by role; feature tests for each entry point, each landing page and each cross-role denial; one verification email observed in Herd's mail catcher (quoted subject line, no addresses); suite count stated — files: app/Enums, app/Models/User, app/Policies, routes, resources/js/pages — ADVISOR (auth boundary, mandatory) before the first edit.
5. Money + Settings — [done in r5: `09be70a`, `71be91e`]. Do not redo. Original done-criteria for reference: `App\Support\Money` (integer fils; add, subtract, percentage, format "AED 120.00", no float leakage) with unit tests; `settings` table with a `group` column (platform|site|mail|features, DATA_MODEL v1.2) + `Settings` facade with cache; unit tests for read, write and cache-bust; suite count stated — files: app/Support, app/Services/Settings, database/migrations, tests/Unit. (Only the column and the platform-group keys land here; the Filament editor and the site/mail/features keys are CP1 per CHECKPOINTS v1.2.)
6. Seeders — [done in r5: `84a81c7`, `f32ede3`; counts 5/25/9/20/1 quoted]. Do not redo. Original done-criteria for reference: curricula (GCSE, A_LEVEL, IB_MYP, IB_DP, CBSE), ≈25 subjects, price bands per curriculum × tier from PRD §3 (only the tiers that exist for that curriculum), platform-group settings from DATA_MODEL.md, admin user per R10; factories for every model created so far; `php artisan migrate:fresh --seed` green on the local `elearning` database only; row counts quoted — files: database/seeders, database/factories.
7. Layout shell + RTL guard — done means: the app layout sets `<html dir>` from the user's locale (always `ltr` in v1) and uses logical utilities only; `scripts/rtl-check.sh` greps `resources/` for `ml-|mr-|pl-|pr-|left-|right-|text-left|text-right` (R9 exclusion rule) and is wired into `composer test` (must run under Git Bash on Windows and on Linux); a VERIFICATION entry quotes it failing on a temporary component using `ml-4` and passing after `ms-4` (temporary component removed and said so) — files: resources/js/layouts, scripts/, composer.json.
8. CI + branch protection + PR — done means: `.github/workflows/ci.yml` on PHP 8.4 runs Pint (`--test`), `composer test` against `postgres:18` + `redis` services, `npm run build`, the RTL check, and one command proving Horizon boots on Linux (e.g. `php artisan horizon:status` against the Redis service) on every PR; `.github/CODEOWNERS` per R7; branch protection per R7 via `gh api` (settings quoted); suite green with the literal count; `git log origin/main..HEAD` quoted; branch pushed; PR #1 opened with the CP0 acceptance list from CHECKPOINTS.md as its checklist; CI green on the PR (run URL quoted) — ADVISOR (mandatory) before opening the PR.
9. Review + halt — done means: fresh-subagent adversarial review of PR #1 logged as REVIEW with numbered verdicts (Low doc-only findings fixed in-cycle with a DEVIATION entry; anything else stops the cycle); STATUS.md rewritten with §7 "Owner action 1: GO to merge PR #1 — reply `update` when done", and §6 stating whether PostgreSQL/Redis are Herd-managed (expected: yes) — halt: yes, owner GO required. This is the cycle's only planned halt.

## Out of scope this cycle
Any CP1+ item from CHECKPOINTS v1.2 (site settings editor, pages, document types, bank details, admin users, registries); Docker in any form; any external service other than GitHub; hosting or a rehearsal server; Arabic translation; any DECISIONS.md row other than ADR-003; editing the four v1.2 docs.

## Carried forward
After PR #1 merges: the CP1–CP2 autonomous programme (owner ruling 2026-09-17) and the Forge rehearsal server (Linux, PHP 8.4, PostgreSQL 18, Horizon for real) as its own cycle. D-02 gateway and D-04 legal entity: by the CP5 plan (gateway registry lands there, Stripe first). D-05 brand: site setting in CP1; domain and sender address by CP6. Human review gating (backend dev before merge on CP3/4/5, UI dev after merge) to be confirmed in the CP3 plan. R16 and R18 to be added to HOW-WE-WORK §14 on the next "refresh the brief". Reference spec pack not yet in docs/reference/.
