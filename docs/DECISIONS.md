# DECISIONS — project-elearning (ADRs)

_Accepted architecture and policy decisions that outlive any single plan. Written by Claude Code only when a plan authorises it; proposals go to STATUS.md §4 first. Cycle-level rulings live in PLAN.md, not here._

| # | Date | Decision | Reason | Status |
|---|---|---|---|---|
| ADR-001 | 2026-09-16 | Local dev environment = Laravel Herd, native Windows, no Docker | Docker Desktop crash-loops on a stale inference socket (docker/desktop-feedback #460) and recovery needs manual steps CC cannot perform; native is also faster | Accepted (owner ruling) |
| ADR-002 | 2026-09-17 | Claude Code permission allow-list (`.claude/settings.local.json`): allow `Bash(git status:*)`, `Bash(git log:*)`, `Bash(git diff:*)`, `Bash(git fetch:*)`, `Bash(git pull:*)`, `Bash(git add:*)`, `Bash(git commit:*)`, `Bash(git branch:*)`, `Bash(git switch:*)`, `Bash(git push:*)`, `Bash(composer.bat:*)`, `Bash(php:*)`, `Bash(vendor/bin/pest:*)`, `Bash(vendor/bin/pint:*)`, `Bash(npm:*)`, `Bash(npx:*)`, `Bash(node:*)`, `Bash(herd.bat:*)`, `Bash(psql:*)`, `Bash(gh api:*)`, `Bash(gh pr:*)`, `Bash(gh run:*)`, `Bash(gh issue view:*)`; deny `Bash(git push --force:*)`, `Bash(git push -f:*)`, `Bash(git push --force-with-lease:*)`, `Bash(git reset --hard:*)`, `Bash(git checkout .:*)`, `Bash(git clean:*)`, `Bash(git branch -D:*)`, `Bash(rm -rf:*)`, `Bash(rm -fr:*)`. `git checkout <branch>` deliberately omitted from allow (cannot be expressed without also matching `git checkout .` / `-- <file>`); use `git switch` instead. | Written but not installed — CC is blocked from creating its own permission-settings file by a "Self-Modification" guardrail below the plan/owner-loop layer; see STATUS.md §7 for the owner action needed to install it |
| ADR-003 | 2026-09-17 | Framework pinned to Laravel 13 / Inertia 3 as installed | Official installer default on install day; Filament 5 accepts it; Laravel 12 is security-only; nothing in the design depends on the major | Accepted (owner ruling) |
| ADR-004 | 2026-09-20 | Year group is a controlled list per curriculum (`year_groups`, 21 seeded rows, admin-managed), not free text: `learners.year_group_id` and `tutor_subjects.level_min_id`/`level_max_id` point at it, the tutor's level tier is derived from the range and never sent by the client, and search filters by id in SQL. The old text stays only in nullable `*_legacy` columns for rows `LegacyYearGroupMapper` could not map; `match_requests.year_group` stays a label snapshot | Free text made search, tier derivation and price-band checks depend on spelling; a list makes them exact and lets the admin correct a label in one place | Accepted (owner ruling R33; shipped in PR #9 `530b5e5`) |
| ADR-005 | 2026-09-19 | Rehearsal environment = Laravel Forge on a Hetzner Cloud CX23 (Falkenstein), named `trustutor-rehearsal`, site `rehearsal.trustutor.com`, database `trustutor_rehearsal`, private network `trustutor-pn`; Ubuntu 26.04, PHP 8.4, PostgreSQL 18, Redis, Let's Encrypt. Push-to-deploy OFF (the owner presses Deploy); zero-downtime deploys ON, so the app lives under `/current`; Horizon is a Forge background process and the scheduler a Forge per-minute job. Claude Code holds no Forge, Hetzner, Cloudflare or GitHub credential; it reaches the box only by one non-interactive ssh shape with a dedicated key and the read-only allow-list in `CLAUDE.local.md`; every state-changing command needs a per-command owner yes. Production will be `trustutor-production` / `trustutor.com`, built from this recipe at CP8 | Every server-script change is rehearsed before production (HOW-WE-WORK §8/§10); a docs-only commit must never reach the box by accident; the naming keeps this project unambiguous among the owner's other servers. Verified read-only in cycle 03 step 4 (env `rehearsal`, debug off, 30/30 migrations, Horizon running, scheduler ticking, seed counts read) | Accepted (owner ruling R41; supersedes R38's provider and OS) |
| ADR-006 | 2026-09-19 | The brand is **TrusTutor**, domain `trustutor.com` (decision D-05); spelled with capital T twice (`site_name` = `TrusTutor`) everywhere; servers, sites, databases and the private network carry the `trustutor-` prefix | One name, one spelling, so the project cannot be confused with the owner's other projects; the `site_name` setting (not `APP_NAME`) is what public pages render, and a spelling check must read that surface | Accepted (owner ruling R40; confirmed on trustutor-rehearsal, R48) |
| ADR-007 | 2026-09-19 | Transactional mail is deferred: trustutor-rehearsal runs `MAIL_MAILER=log`, so nothing sends and no email from that box is evidence of anything. Choosing and wiring a provider is decision D-09, its own step in a later cycle (recommendation when it comes up: Postmark or Resend, one sending domain `trustutor.com`, SPF/DKIM in Cloudflare, sender address from the `mail` settings group so it stays admin-editable, PRD §12). Until then no plan may claim an email path is verified end to end; the rehearsal verifies the queue, not delivery | Provider choice, domain authentication and a real sending identity are owner decisions and need a real sender; pretending the log driver proves delivery would hide the gap until CP6 | Accepted (owner ruling R43); D-09 open |
| ADR-010 | 2026-09-23 | `docs/HOW-WE-WORK.md` v1.1 adopted in full (merged from v1.0 with a project-generic operating method: window/session/cycle vocabulary, advisor proof — every ADVISOR entry names the responding model or is a tool failure that does not count, a HANDOFF log kind at every session close, `STATUS.md`'s header carrying a measured `Context: <n>k` figure read from `/context`, never estimated, §15 cost/context discipline: 200k window, one PLAN step per session, 100k ceiling inside a step); adopted unchanged as the planner placed it, 443 lines. `CLAUDE.md`'s `### Owner loop` block is replaced with v1.1's Appendix B verbatim (R60), `<repo path>` filled as `C:\\project elearning`; nothing else in `CLAUDE.md` changes | The three operative changes close real gaps seen in cycle 03/04: an ADVISOR entry with no named model cannot be told apart from a skipped consult; a session that stops mid-step with no HANDOFF leaves the next session to reconstruct state from code alone; an unmeasured context claim in STATUS is not verifiable | Accepted (owner ruling R58–R60, PLAN cycle 04 r3) |
