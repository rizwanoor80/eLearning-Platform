# DECISIONS — project-elearning (ADRs)

_Accepted architecture and policy decisions that outlive any single plan. Written by Claude Code only when a plan authorises it; proposals go to STATUS.md §4 first. Cycle-level rulings live in PLAN.md, not here._

| # | Date | Decision | Reason | Status |
|---|---|---|---|---|
| ADR-001 | 2026-09-16 | Local dev environment = Laravel Herd, native Windows, no Docker | Docker Desktop crash-loops on a stale inference socket (docker/desktop-feedback #460) and recovery needs manual steps CC cannot perform; native is also faster | Accepted (owner ruling) |
| ADR-002 | 2026-09-17 | Claude Code permission allow-list (`.claude/settings.local.json`): allow `Bash(git status:*)`, `Bash(git log:*)`, `Bash(git diff:*)`, `Bash(git fetch:*)`, `Bash(git pull:*)`, `Bash(git add:*)`, `Bash(git commit:*)`, `Bash(git branch:*)`, `Bash(git switch:*)`, `Bash(git push:*)`, `Bash(composer.bat:*)`, `Bash(php:*)`, `Bash(vendor/bin/pest:*)`, `Bash(vendor/bin/pint:*)`, `Bash(npm:*)`, `Bash(npx:*)`, `Bash(node:*)`, `Bash(herd.bat:*)`, `Bash(psql:*)`, `Bash(gh api:*)`, `Bash(gh pr:*)`, `Bash(gh run:*)`, `Bash(gh issue view:*)`; deny `Bash(git push --force:*)`, `Bash(git push -f:*)`, `Bash(git push --force-with-lease:*)`, `Bash(git reset --hard:*)`, `Bash(git checkout .:*)`, `Bash(git clean:*)`, `Bash(git branch -D:*)`, `Bash(rm -rf:*)`, `Bash(rm -fr:*)`. `git checkout <branch>` deliberately omitted from allow (cannot be expressed without also matching `git checkout .` / `-- <file>`); use `git switch` instead. | Written but not installed — CC is blocked from creating its own permission-settings file by a "Self-Modification" guardrail below the plan/owner-loop layer; see STATUS.md §7 for the owner action needed to install it |
| ADR-003 | 2026-09-17 | Framework pinned to Laravel 13 / Inertia 3 as installed | Official installer default on install day; Filament 5 accepts it; Laravel 12 is security-only; nothing in the design depends on the major | Accepted (owner ruling) |
| ADR-004 | 2026-09-20 | Year group is a controlled list per curriculum (`year_groups`, 21 seeded rows, admin-managed), not free text: `learners.year_group_id` and `tutor_subjects.level_min_id`/`level_max_id` point at it, the tutor's level tier is derived from the range and never sent by the client, and search filters by id in SQL. The old text stays only in nullable `*_legacy` columns for rows `LegacyYearGroupMapper` could not map; `match_requests.year_group` stays a label snapshot | Free text made search, tier derivation and price-band checks depend on spelling; a list makes them exact and lets the admin correct a label in one place | Accepted (owner ruling R33; shipped in PR #9 `530b5e5`) |
| ADR-005 | 2026-09-19 | Rehearsal environment = Laravel Forge on a Hetzner Cloud CX23 (Falkenstein), named `trustutor-rehearsal`, site `rehearsal.trustutor.com`, database `trustutor_rehearsal`, private network `trustutor-pn`; Ubuntu 26.04, PHP 8.4, PostgreSQL 18, Redis, Let's Encrypt. Push-to-deploy OFF (the owner presses Deploy); zero-downtime deploys ON, so the app lives under `/current`; Horizon is a Forge background process and the scheduler a Forge per-minute job. Claude Code holds no Forge, Hetzner, Cloudflare or GitHub credential; it reaches the box only by one non-interactive ssh shape with a dedicated key and the read-only allow-list in `CLAUDE.local.md`; every state-changing command needs a per-command owner yes. Production will be `trustutor-production` / `trustutor.com`, built from this recipe at CP8 | Every server-script change is rehearsed before production (HOW-WE-WORK §8/§10); a docs-only commit must never reach the box by accident; the naming keeps this project unambiguous among the owner's other servers. Verified read-only in cycle 03 step 4 (env `rehearsal`, debug off, 30/30 migrations, Horizon running, scheduler ticking, seed counts read) | Accepted (owner ruling R41; supersedes R38's provider and OS) |
| ADR-006 | 2026-09-19 | The brand is **TrusTutor**, domain `trustutor.com` (decision D-05); spelled with capital T twice (`site_name` = `TrusTutor`) everywhere; servers, sites, databases and the private network carry the `trustutor-` prefix | One name, one spelling, so the project cannot be confused with the owner's other projects; the `site_name` setting (not `APP_NAME`) is what public pages render, and a spelling check must read that surface | Accepted (owner ruling R40; confirmed on trustutor-rehearsal, R48) |
| ADR-007 | 2026-09-19 | Transactional mail is deferred: trustutor-rehearsal runs `MAIL_MAILER=log`, so nothing sends and no email from that box is evidence of anything. Choosing and wiring a provider is decision D-09, its own step in a later cycle (recommendation when it comes up: Postmark or Resend, one sending domain `trustutor.com`, SPF/DKIM in Cloudflare, sender address from the `mail` settings group so it stays admin-editable, PRD §12). Until then no plan may claim an email path is verified end to end; the rehearsal verifies the queue, not delivery | Provider choice, domain authentication and a real sending identity are owner decisions and need a real sender; pretending the log driver proves delivery would hide the gap until CP6 | Accepted (owner ruling R43); D-09 open |
| ADR-008 | 2026-09-21 | The escrow ledger (`ledger_entries`, `LedgerService`) is pulled forward from CP5 into CP3, alongside the state machine, rather than built after booking lands | CP3's acceptance requires a refund path and a tutor-paid path, and those are fiction as status changes alone without entries to prove money actually moved; building the append-only ledger now, with HOLD/RELEASE/REFUND live and SETTLE/PAYOUT declared but unused, keeps CP4/CP5 additive rather than requiring a later retrofit | Accepted (owner ruling R52, PLAN cycle 04 r1; shipped in PR #12, `5471bcb`) |
| ADR-009 | 2026-09-21 | Deletion policy: a learner is soft-deleted only when they have no lesson in a non-terminal state (refused otherwise, naming the blocking lessons); a user is never hard-deleted — deletion anonymises identity fields and revokes credentials/sessions/tokens but leaves `lessons`, `payments` and `ledger_entries` intact with their foreign keys | Financial history must outlive the person; hard-deleting a party to a lesson would break the ledger's referential integrity and the audit trail money invariants depend on | Accepted (owner ruling R54, PLAN cycle 04 r1) |
| ADR-010 | 2026-09-23 | `docs/HOW-WE-WORK.md` v1.1 adopted in full (merged from v1.0 with a project-generic operating method: window/session/cycle vocabulary, advisor proof — every ADVISOR entry names the responding model or is a tool failure that does not count, a HANDOFF log kind at every session close, `STATUS.md`'s header carrying a measured `Context: <n>k` figure read from `/context`, never estimated, §15 cost/context discipline: 200k window, one PLAN step per session, 100k ceiling inside a step); adopted unchanged as the planner placed it, 443 lines. `CLAUDE.md`'s `### Owner loop` block is replaced with v1.1's Appendix B verbatim (R60), `<repo path>` filled as `C:\project elearning`; nothing else in `CLAUDE.md` changes | The three operative changes close real gaps seen in cycle 03/04: an ADVISOR entry with no named model cannot be told apart from a skipped consult; a session that stops mid-step with no HANDOFF leaves the next session to reconstruct state from code alone; an unmeasured context claim in STATUS is not verifiable | Accepted (owner ruling R58–R60, PLAN cycle 04 r3) |
| ADR-010 addendum | 2026-09-23 | Advisor-model citation, decided (R63): the advisor tool does not report which model answered a call, so ADR-010's "names the responding model" requirement is satisfied, from cycle 04 onward, by the fixed phrase "Model: not reported by the tool; configured advisor per PROJECT_BRIEF is Fable 5.1 — configured, not measured." An entry carrying that phrase counts toward the sub-cycle advisor minimum and the mandatory money triggers | The tool genuinely cannot report a per-call model, so requiring one as a measured fact would make every ADVISOR entry non-compliant by construction; a fixed, honest "configured, not measured" phrase closes the gap without inventing data | Accepted (owner ruling R63, PLAN cycle 04 r3) |
| ADR-011 | 2026-09-23 | `docs/HOW-WE-WORK.md` v1.2 adopted in full, superseding v1.1 (ADR-010): the owner clears only at cycle END, when STATUS §7 raises the clear action, not at every PLAN step; auto-compaction inside a cycle is normal and is never itself a reason to stop; §12's 100k in-step ceiling and the programme's context cap are both removed; `STATUS.md`'s `Context:` figure stays a measured, reported fact but is no longer a threshold CC stops against. Adopted unchanged as the planner placed it, 446 lines (v1.2 plus the seven TrusTutor §14 lines carried from v1.1, marked as such). `CLAUDE.md`'s `### Owner loop` block is replaced with v1.2's Appendix B verbatim (R68), landing inside PR #12's diff; nothing else in `CLAUDE.md` changes | v1.1's per-step clear cost more owner time than it saved in tokens, and this window's fixed harness overhead (~83k tokens before any project file is read, measured three times on 2026-09-23) made the 40k/100k ceilings unreachable — every fresh session under v1.1/r4 was forced to stop before landing any work, five stops in one day with zero work done. v1.2 removes the ceiling that caused this rather than tuning its threshold | Accepted (owner ruling R67–R69, PLAN cycle 04 r5; r4's R64 withdrawn as the planner's bug) |
| ADR-012 | 2026-09-25 | Session clearing (R86, owner ruling 2026-09-25): CC never stops, halts or raises an Owner action only to clear the session; a clear is offered only at a halt that already needs the owner (GO, question, Deploy, blocker, or END with nothing further authorised), where STATUS.md is written handoff-grade and the Owner action carries the exact reply, "then `/clear` this session and reply `update — <answer>`". Supersedes HOW-WE-WORK v1.2 rule 13 and §15 where they differ; `CLAUDE.md` Owner-loop rule 13 is replaced with R86(d)'s text in 4a's PR; HOW-WE-WORK.md stays unchanged until the owner issues v1.3 | The old rule (one cycle per session, clear at END) made the owner a clock for the programme: a multi-step autonomous programme stalled at every cycle boundary although nothing needed a decision. Compaction plus a handoff-grade STATUS.md already make continuing safe; a clear only earns its cost when the owner is being asked something anyway | Accepted (owner ruling) |
| ADR-013 | 2026-09-25 | `payments` holds one row per charge attempt, not one per lesson (R101, 4e): v1.4's `unique (lesson_id)` is replaced by `attempt_no` with `unique (lesson_id, attempt_no)` and a partial unique index `payments_one_live_per_lesson` on `(lesson_id) WHERE status <> 'failed'`, so a lesson has any number of failed attempts and at most one live one; the auto-charge gateway idempotency key is `lesson:{id}:attempt:{n}`; `payments.payment_method_id` gains its foreign key (restrict). `Lesson::payment()` is the highest `attempt_no` | A weekly lesson is charged up to three times and each failure must stay visible (audit and `ledger:verify`); a real gateway caches a failed result per idempotency key, so a retry needs a fresh key; the database, not the action, must be what forbids two live payments for one lesson | Accepted (PLAN cycle 05 R101; DATA_MODEL v1.5) |
| ADR-014 | 2026-09-25 | Auto-charge policy for weekly lessons (R101, 4e): attempts at `starts_at` minus 48h, 36h and 24h (`recurring_charge_lead_hours`, `recurring_retry_hours`), each at least an hour after the previous and only if before the start; success confirms the lesson and writes the HOLD in the same transaction (no new ledger operation, no new state edge); the third failure cancels as `cancelled_payment_failed` and counts once against the slot, and `recurring_pause_after_failures` (2) consecutive counted failures pause the slot with reason `payment_failed`; a lesson whose start has passed uncharged is cancelled `charge_window_missed` and does not count against the slot | The parent's card and the platform's timing are different faults; a late-generated lesson must not fire two attempts in one hour; money moves only through `LedgerService` inside the state-machine transition | Accepted (PLAN cycle 05 R101; owner-reviewed at the 4e backend-dev GO) |
| ADR-015 | 2026-09-25 | A parent may resume a slot the system paused for failed charges (`payment_failed`), only after replacing the declined card; an admin-paused slot stays admin-only to resume; `PaymentGateway` stays unbound outside tests until CP5, so `recurring:charge` on a server with no gateway charges nothing, says so, exits 0, and still cancels lessons whose start has passed | Otherwise a parent who has fixed their card waits on staff; binding a gateway before CP5's registry would break invariant 16 | Accepted (PLAN cycle 05 R101; 3c DECISION on the gateway binding) |
| ADR-016 | 2026-09-26 | The fake payment gateway is bound to `PaymentGateway` by an environment allow-list (R107, 4f): `App\Providers\PaymentGatewayServiceProvider` binds `FakePaymentGateway` only on `local`, `testing` and `rehearsal`; `production`, `staging` and any unlisted or missing `APP_ENV` leave the interface unbound and resolving it throws. One predicate, `fakeGatewayAllowed()`, also drives the "Test mode" banner and `SaveTestCard::available()`. This supersedes ADR-015's clause that the gateway "stays unbound outside tests until CP5"; the rest of ADR-015 stands. CP5's `payment_gateways` registry replaces this binding | The rehearsal server needs the weekly auto-charge to run end to end on fake money, and invariant 16 forbids hard-wiring a driver, so the stopgap is an allow-list rather than a deny-list: a typo or a new environment fails closed and can never fake-charge a real parent. `artisan --env=…` overrides `APP_ENV` for that process, so it is never passed on a server (CLAUDE.local.md already forbids it) | Accepted (PLAN cycle 05 R107) |
| ADR-018 | 2026-09-26 | Owner rulings on the three open decisions (R120): **D-09 mail provider = Postmark** (ADR-007 already named it; wiring — sending domain, SPF/DKIM in Cloudflare, API key in the admin — is its own later step, and rehearsal stays on `MAIL_MAILER=log`, R43, so no plan claims delivery until then). **D-02 payment gateway = multiple gateways**, all integrated through CP5's `payment_gateways` registry with keys entered in the admin (PRD §12); **Stripe is the first real driver**, provisional until D-04 confirms account eligibility; other drivers (PayPal, a UAE-local gateway) follow as separate cycles on request. **D-04 legal entity: open** — the owner takes it to counsel; nothing built this cycle depends on it | The registry is what makes "which gateway" data rather than code (PRD §12, invariant 16), so choosing several costs one driver per provider and no schema change; Stripe leads because it supports saved-card charging, which weekly slots need; D-04 gates the gateway account, VAT and the tutor agreement, so it cannot be settled by the build | Accepted (PLAN cycle 07 R120) |
