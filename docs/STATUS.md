# STATUS — cycle 03 r3 — written 2026-09-21 01:20
Tests: 607 tests / 2830 assertions (main `6214ad4`, `COMPOSER_PROCESS_TIMEOUT=1200 composer.bat test`; nothing in code changed since) · Advisor: cycle 03 consulted 8 times (step 1 design 09:30, pre-PR 11:30; step 2 design 13:30, pre-PR 17:10; step 3 design 19:45, pre-PR 22:10; step 4 access 00:08; step 4 result 01:08) · Review: step 3 done (0 Medium/High, 5 Low)

## §1 Git state
`main` = PLAN r3 commit `33e4ba0` on top of `50d53ee`, plus this docs commit; code is at `6214ad4` (PR #10, step 3, merged under R39); no open branch or PR. trustutor-rehearsal runs release `50d53ee` (first deploy); it is behind `main` by docs-only commits, which is expected — push-to-deploy is OFF (R41) and no deploy is triggered. Suite 607 / 2830, Pint, PHPStan 0, RTL, `npm run build` exit 0, curl smoke (`/`, `/tutors`, `/tutors?curriculum_id=1&year_group_id=1`, `/login`, `/admin/login` 200; `/dashboard` 302) all green on merged main. Branch protection on `main`: **not set** (owner action, carried, R23). `.claude/settings.local.json` (R14): not created — optional, carried.

## §2 Step map (cycle 03 r3 — "CP2.5")
1. `cp/2.5a-rulings` — [done] PR #8 merged `a6281eb`.
2. `cp/2.5b-year-groups` — [done] PR #9 merged `530b5e5` (fresh review 0 Medium/High, 5 Low; CI green on the head).
3. `cp/2.5c-tutor-lifecycle` — [done] PR #10 merged `6214ad4` (fresh review 0 Medium/High, 5 Low; CI green on the head) — R36 (a)–(g) in full plus the six named Lows.
4. Verify trustutor-rehearsal — [HALTED on a mismatch] the shell route works and most of the checks pass (env `rehearsal`, debug OFF, PHP 8.4.25, database `trustutor_rehearsal`, Horizon running, 30/30 migrations Ran, no failed jobs, HTTPS 200 on four pages); the plan halts on any mismatch and there is one: the site name reads `trusTutor`, R40 says `TrusTutor`. Two items are unproven: the scheduler's last run and four seed counts (CYCLE-LOG 01:10).
5. Close — [not started] (R44 docs, including step-1 Low L3; halt).

## §3 What changed this run
- This run (`update` after Owner actions 1–2): read-only verification of trustutor-rehearsal over the owner-provisioned ssh route (ten allow-listed commands, quoted in CYCLE-LOG 01:10); no code, no server change, nothing deployed. Earlier this cycle: PLAN r3 committed (`33e4ba0`), rulings R40–R44 logged, public HTTPS checks passed.
- Step 1 closed (merged `a6281eb`). Step 2 built in three commits: **table, mapper, seeder, report** (`year_groups` with 21 seeded rows from one source; `LegacyYearGroupMapper`; `php artisan year-groups:report [--remap]`, names-free; `docs/reports/year-groups-migration.md`); **learners, onboarding, search, match requests** on the list (year-group ids validated against the curriculum; onboarding takes min/max year-group ids and derives the tier, a client-sent tier is dropped; unmapped rows send a draft/changes_requested tutor back to the subjects step; search filters by `year_group_id` in SQL; match requests store the chosen label as a snapshot); **Filament CRUD** (admin-only, audited, curriculum fixed after creation, tier limited to the curriculum's tiers, delete hidden while referenced, tier/sort edits re-derive stored tiers with one audit row) plus L2 (00:00 UTC boundary test) and L4 (leading zero-width characters stripped from `displayName()`).
- Tests: 34 net new (462 / 2383 assertions), including a rollback → legacy rows → migrate round trip.
- Step 3 built in three slices plus fixes: **(1)** `TutorStatusTransitions` (the one table of allowed tutor-status edges; every tutor-status action asserts through it inside a transaction on a locked row and dispatches after commit), `ReinstateTutor` (suspended → approved if permit valid + every required document accepted + rate in band, else changes_requested with reasons and the email; audited), approved → changes_requested ("Request changes" is now offered on approved tutors), a rejected document on an approved tutor and a newly required document type move approved tutors to changes_requested with the existing email, resubmit clears `review_note` and stamps the new `submitted_at` (queue sorts on it, backfilled from `created_at`), `TutorRateBands` extracted from the onboarding controller, approval re-checks permit, documents and rate (`TutorProfile::permitIsValid()` agrees with `bookable()` on yesterday/today/tomorrow); **(2)** replaced document files deleted after the replacement is accepted (and a failed upload row deletes its own file), a permit number/date edit resets an accepted permit scan to pending (audited; `DocumentType::PERMIT_CODE`), permit job `onOneServer()->withoutOverlapping()`; **(3)** Fortify verify/reset mails from the settings sender and queued, transactional last-active-admin check (active admin rows locked), duplicate price band refused with a message, document-type `code` (slug, unique, immutable on edit) and `sort` (0–32767) validated, admin Disable confirmation; **fix** `ReviewTutorDocument` decides on the locked profile in one transaction (advisor #6).
- Tests: +145 net new (607 / 2830).
- Review of PR #9 and merge: a fresh subagent gave no Medium/High; PR #9 merged under R39 (`530b5e5`); post-merge suite, build and curl smoke green (CYCLE-LOG 19:20).

## §4 Decisions
- R39: PR #8 merged now; PRs #9 and #10 merge without a further GO only if: fresh review with no open Medium/High, CI green on the PR head, diff limited to the step's named areas plus tests/migrations/seeders/factories/routes/resources, and none of CLAUDE.md, HOW-WE-WORK.md, PRD.md, `ci.yml` changed. Any Medium+ → PR stays open and the run halts.
- Step 2 semantics: CYCLE-LOG DECISION 13:31 and 17:11.

## §5 Why stopping
Step 4 halts under its own rule ("any mismatch halts"). One confirmed mismatch: the site name is `trusTutor`, R40 requires `TrusTutor` (the admin Settings field `site_name`, served as the page title and the `name` prop; `about`'s `TrusTutor` is `APP_NAME`, a different source). Two items are unproven with the allow-list: the scheduler's last run (`schedule:list` shows the entry defined, `laravel.log` does not exist yet) and the seed counts for price bands, settings, the admin and content blocks. I changed nothing on the box. Step 5's ADR-005 records step 4's outcome, so it waits.

## §6 Mismatches
- **Own error, disclosed (CYCLE-LOG 00:05):** my 23:40 STATUS and log restated R38's wrong facts (DigitalOcean, Ubuntu 24.04); R41 records what was built (Hetzner CX23, Ubuntu 26.04). I did not check them against anything but the plan.
- **trustutor-rehearsal is behind `main`, by design:** it runs `50d53ee`; `main` has docs-only commits since (push-to-deploy OFF, R41). Not drift. No deploy will be triggered to close it.
- **Site name casing (step 4 mismatch, blocks step 4):** the admin setting `site_name` reads `trusTutor`; R40 requires `TrusTutor`. Owner action 1.
- **Scheduler last run unproven:** `schedule:list` shows `0 6 * * * tutors:check-permits` defined (next due in 11 hours); `storage/logs/laravel.log` does not exist (only `.gitignore` in `storage/logs`), so nothing corroborates that the Forge per-minute job has fired. Consistent with a healthy silent app; proof is Forge's scheduled-job page (Owner action 2).
- **Seed counts partly unproven:** proven publicly: 5 curricula, 25 subjects, 21 year groups, 6 footer pages. Not reachable read-only: 9 price bands, 20 settings, 1 admin, content blocks (Owner action 2). No `tinker`, `-r` or raw SQL was used.
- **Server `git status` (Low, not touched):** `D storage/app/.gitignore` and `D storage/app/private/.gitignore` on the release — an artefact of the zero-downtime `storage` symlink, not drift.
- **`about` shows `Broadcasting reverb`** (configured, not run); the CP7 plan owns Reverb. Cache and Session drivers are `database`. Noted only.
- **Ten unfixed Lows are ruled by R42:** they go to a named Lows pass at the front of the CP3 plan, worst first (year-group `withTrashed()` 500 and `sort` bound; Livewire tier mismatch; adult-learner curriculum change; `Onboarding.vue` index lookup; then step 3's five). They stay listed below until that pass closes them.
- **Stale plan text still to correct in step 5:** R37 (superseded by R44) and R38 (superseded by R41); ADR-005 is written from R41.
- **Branch protection on `main` not set** (owner action, carried; R23).
- **CP1 box 7 admin-payout clause deferred to CP5** (R25) — "CP1 done" stays qualified.
- **Step 2 deviations (disclosed):** `*_legacy` text columns are kept for unmapped rows (R33 says the columns "become foreign keys"; DATA_MODEL has no legacy columns — for the v1.3 note in step 5); the legacy mapping is trimmed, whitespace-collapsed, case-insensitive exact-label (R33: "exact label"); CBSE labels "Grade 6"–"Grade 12" are my default; `match_requests.year_group` stays a label snapshot; "left null for admin correction" has no admin path — the row's owner corrects it (parent via the learner form, draft/changes_requested tutor via onboarding) and an approved tutor's unmapped row is permissive in search until step 3's re-vetting (R36b); an admin's tier/sort edit can push an approved tutor's rate out of band and nothing re-checks until R36(f) (step 3) and CP3 at booking; `LegacyYearGroupMapper::norm()` is Postgres-specific SQL; `->after('is_minor')` is a no-op on Postgres; no unique on `(curriculum_id, sort)` (a range with two equal sorts is ambiguous; the CRUD does not prevent it — a Low); `YearGroupOptions::all()` ships all 21 rows to four pages; the Filament "delete refused if called anyway" path is unreachable in a test (a hidden action cannot be called) — the visibility guard and the foreign key are tested; `year-groups:report --remap` is state-changing and needs a per-command "yes" on the rehearsal server.
- **Test integrity (step 2):** the `TutorSearchTest` free-text parsing test is removed because R33 deletes that behaviour, and replaced by id-based tests; `TutorOnboardingBankSubjectsRateTest`'s "level tier that does not exist for the chosen curriculum" test is reshaped into "year groups that belong to another curriculum" (the client can no longer send a tier); onboarding subjects POSTs go through `ygRow()`, learner setup through `lnChild()`; `MatchRequestTest`'s snapshot test is stronger; `DatabaseSeederTest` +21.
- **Diff-scope files not pre-declared by name (all inside the step's areas):** `YearGroupTiers.php`, `YearGroupOptions.php`, `RederiveSubjectTiers.php`, `UpdateLearner.php`, `TutorProfileController.php`, `SendMatchSuggestionsMail.php`, `CreateLearner.php`, `TutorProfile.php` (L4), three test files; pre-declared but untouched: `MatchRequestFactory.php`, `HandleInertiaRequests.php`, `tutors/Show.vue`.
- **Step 2 review Lows (unfixed, merged; code changes return to the owner per protocol §6):** (a) `EditYearGroup.php:32` `isReferenced()` ignores soft-deleted learners while the foreign key counts them — a parent soft-deletes a child, the admin's Delete then throws `QueryException` (500), and the `year_group.deleted` audit row written in `before()` may persist for a record that was not deleted (fix: `withTrashed()`; the reviewer said it might be weighed as Medium — I took Low: admin-only, no data lost, FK held); (b) `YearGroupForm.php:60` tier options follow the client-sent `curriculum_id` that `mutateFormDataBeforeSave` drops — a crafted Livewire request can save a tier that does not belong to the curriculum; (c) migration line 22 `sort` is smallint with no form `maxValue` — `sort` 40000 gives a 500; (d) `UpdateLearnerRequest.php:34` an adult student's own learner can change curriculum without `year_group_id` and keep the old year group (crafted request only); (e) `Onboarding.vue` unmatched-legacy text is looked up by array index (shifts after removing a row). Also noted: `YearGroupDefaults::insertMissing()` keys on code while the unique also covers label (contrived); coverage gaps — no test that ONE subject row satisfies curriculum+subject+year group together (behaviour probed and holds), item #12 tested for a draft tutor only, the `storeSubjects` "tier not in the curriculum's tiers" branch is unreachable from a client.
- **Composer timeout:** plain `composer.bat test` ran 473 s on merged main and hit composer's default 300 s process timeout once (rerun with `COMPOSER_PROCESS_TIMEOUT=900` passed). Not a test failure; may recur.
- **Step 3 stale plan text (disclosed before the first edit, CYCLE-LOG 19:50):** (1) the plan's "`MailFromSettings`" does not exist — the settings sender is the trait `App\Support\Mail\UsesSettingsSender`; Fortify's verify/reset mails are stock Laravel notifications, so step 3 adds two queued subclasses using the trait; (2) the plan's "`submitted_at`" column does not exist (the queue's "Submitted" column shows `created_at`) — step 3 adds it; (3) step 2's STATUS said an approved tutor's unmapped year-group row would be re-vetted "in step 3's R36b" — R36b covers only document rejection and a newly required document type, so that was my over-read: an approved tutor's unmapped row stays permissive in search (no production data; local report shows 0 unmatched). Also pre-disclosed: `PasswordResetTest` (3 assertions) and `VerificationNotificationTest` (1) name the stock notification class and are retargeted to the subclass; document-type `code` becomes immutable on edit; the "Request changes" button becomes available on approved tutors; `pending_review` tutors missing a newly required document are not moved (owed).
- **Own error, step 3 (CYCLE-LOG 19:47):** I killed a `artisan test --parallel --processes=4` process I had not started, assuming it was a leftover of a subagent; its source is unconfirmed and could have been the owner's. Owner: if you had a parallel test run going, re-run it.
- **Step 3 test integrity (all disclosed here and in the PR body):** `TutorApprovalActionsTest`: "refuses to reject or request changes on a profile that was never submitted" now uses a suspended profile instead of an approved one (an approved tutor can be sent back since R36 b); "refuses to review a document once the profile is approved, rejected or suspended (R31)" loses its `approved` dataset case (approved is now re-vetting); "leaves an approved tutor bookable and their documents untouched when an admin tries to reject one (R31)" is REMOVED and replaced by its opposite, "moves an approved tutor to changes requested, not bookable, when an admin rejects one of their documents (R36 b)" — R36 b supersedes R31 for approved tutors; two happy-path approvals gain `->approvable()` (approval now needs a subject, a band and a rate inside it). `TutorProfileResourceTest`: "hides accept and reject on documents of an approved tutor (R31)" becomes a suspended-tutor test and a new approved-visible test is added; the approve happy path gains `->approvable()`. `DocumentTypeWizardIntegrationTest`: `TutorProfileFactory::makeApprovable()` before its final approval. `YearGroupMigrationTest`: `--step 1` became a computed count of migrations from the year-groups one onward — my own step-2 test only worked while that migration was the newest. `PasswordResetTest` (3) and `VerificationNotificationTest` (1): assertions retargeted from the stock notification class to the subclass (NotificationFake keys on the exact class). Step 2 review's vacuous-test concern applied to my own first draft: two vacuous assertions in `AuthMailSenderTest` were removed on the advisor's advice.
- **Step 3 deviations and gaps (disclosed):** the plan's `MailFromSettings` does not exist — the trait `UsesSettingsSender` is used (see the earlier entry); document-type `code` is immutable on edit (beyond "validated"); an approved tutor has no permit-edit path (`storePermit` is reachable only in draft/changes_requested), which is consistent with R36 b; events are dispatched explicitly after the transaction rather than via `ShouldDispatchAfterCommit` (a test proves the transaction level at dispatch equals the baseline); a genuine concurrent race cannot be proven in Pest, so the last-admin and status locks are proven by structure (locked select inside the transaction) and by stale-copy tests; `pending_review` tutors missing a newly required document type are NOT moved and can be neither approved nor upload (the admin can use "Request changes", which is allowed) — owed to a later plan; a price-band edit and switching `required` off re-derive nothing; my exploration report claimed `document_types.sort` is `unsignedSmallInteger` — on Postgres that is `smallint` (max 32767), which a test caught, so the form maximum is 32767; the same unbounded-`sort` Low from step 2 (year groups) is still open (ruled by R42, CP3 Lows pass); the "vanishes" test is an HTTP test on the public profile page (200 → 404), not on `/tutors` search (search-side proof of invariant #5 is step 2's). Files not pre-declared by name: `TutorApprovalReadiness.php`, `ResetPermitScanOnPermitChange.php`, `tests/Feature/YearGroups/YearGroupMigrationTest.php`, `ReinstateTutor`-related Filament page edits are inside the declared `TutorProfiles/**`; all inside R39's areas.
- **Step 3 review Lows (unfixed, merged; code changes return to the owner per protocol §6):** (a) `ReviewTutorDocument` does not gate the move on the document's type or state — a stale-page reject of an already-rejected document of an inactive optional type on an approved tutor moves the tutor to changes_requested (the UI hides Reject on a rejected row; an inactive type also leaves no wizard step); (b) `RequireDocumentTypeFromApprovedTutors` catches only `TutorStatusTransitionException`, so any other exception midway leaves later tutors unmoved with no retry short of toggling the flag; (c) `2030-01-05 10:00:00` with the same date resets an accepted permit scan spuriously (unnecessary re-review, not a bypass); (d) the reset token now sits in the queue payload and `failed_jobs` (the stock synchronous mail put it nowhere); (e) `DisableAdminUser` compares ids with strict `===` — int on Postgres today; `whereKeyNot` would be safer; trivial: `review_note` is nulled on a clean reinstate, so the suspension reason survives only in the audit row; coverage note: nothing tests the `lockForUpdate` in `transition()` itself, and the after-commit test is meaningful for `ApproveTutor` only.
- **Step 1 review Lows:** L2 and L4 were fixed in step 2; L3 (DATA_MODEL/`ContentBlock` docblock still say homepage-only) is for step 5's docs pass.
- **Remaining follow-up list:** step 3 (R36 lifecycle plus the Fortify mail sender, "Submitted" sort column, transactional last-admin check, duplicate price-band, document-type `code`/`sort` validation, admin Disable confirmation). Staying on the list for CP3's plan: `whereDate` in `bookable()` may bypass the new index; in-memory search pagination; the onboarding trial-price preview rounds `100 − pct` (CP3 must unify with `trialPrice()`); 2a Lows (user-delete cascade decision, overlapping-offer race, learner delete-guards); 2c Lows L2 (last-writer-wins page edits) and L4 (onboarding agreement text shows raw markdown); double queue hop; `footerPages` query per response; finding #9 disputed.
- **Owed downstream (CP3):** freeze the trial price from `trialPrice()`, validate `bookable()` and the permit at booking, decide user-delete behaviour; CP7 maintains `rating_avg`/`rating_count`.

## §7 Next step and owner actions
Step 4 is halted on the site-name casing and two unproven items; step 5 (docs-only close) follows once they are settled. The previous Owner actions 1 (shell route) and 2 (site name) are done as far as evidence shows: the ssh route works and the name changed, but not to the required casing. Each action ends: reply `update` when done.

- **Owner action 1: correct the site name casing.** Option 1 (Recommended): in `https://rehearsal.trustutor.com/admin/login` → Settings, change the site name from `trusTutor` to `TrusTutor` (capital T first, capital T second, rest lowercase) and save — R40 fixes the brand spelling and this value is the page title and the header of every page. Option 2: tell me in the reply that `trusTutor` is intended, and I will propose an R40 wording change in the plan, not silently accept it. Reply `update` when done.
- **Owner action 2: settle the two unproven items with two looks in a browser, no shell.** (a) In Forge → server `trustutor-rehearsal` → Scheduler, open the `schedule:run` job and confirm it shows a last run within the last two minutes (or write that it does not). (b) In the admin UI, note the row counts of price bands, settings, users with the admin role, and content blocks (expected: 9 / 20 / 1 / the pages' blocks) and write them, with the Forge answer from (a), in a new file `docs/reports/trustutor-rehearsal-verify.md` (counts and a yes/no only; no secrets). Reply `update` when done.
Carried, optional: Owner action A (R7 branch protection on `main`, exact GitHub UI steps in cycle 01's CYCLE-LOG 18:35 BLOCKER); Owner action B (`.claude/settings.local.json`, ADR-002).

## §8 Cycle 03 board
| Step | State | Branch | PR | Review verdict | Owner GO / merge |
|---|---|---|---|---|---|
| 1 rulings + Lows | **merged** | `cp/2.5a-rulings` | [#8](https://github.com/rizwanoor80/eLearning-Platform/pull/8) | 1 round; 11 verdicts PASS/PASS WITH NOTE, 4 Low, no Med/High | GO (R39); merged `a6281eb` |
| 2 year groups (R33) | **merged** | `cp/2.5b-year-groups` | [#9](https://github.com/rizwanoor80/eLearning-Platform/pull/9) | 1 round; 12 verdicts, 0 Med/High, 5 Low | advance GO (R39) exercised; merged `530b5e5` |
| 3 tutor lifecycle (R36) | **merged** | `cp/2.5c-tutor-lifecycle` | [#10](https://github.com/rizwanoor80/eLearning-Platform/pull/10) | 1 round; 13 verdicts, 0 Med/High, 5 Low | advance GO (R39) exercised; merged `6214ad4` |
| 4 verify trustutor-rehearsal (R41) | **halted on a mismatch: site name `trusTutor` (Owner action 1); scheduler last run + 4 seed counts unproven (Owner action 2)** | — | — | — | owner gate |
| 5 close (R44 docs) | not started | — | — | — | docs-only to `main` |

Cycle 02 (programme CP1–CP2) is complete: sub-cycles 1a `c08bad8`, 1b `10e6198`, 1c `407a687`, 2a `4bae31a`, 2b `1c5ee05`, 2c `b0af825` (PRs #2–#7).

## Deferred (out of v1 scope — do not build)
- Lesson packs / subscriptions / credits
- Group classes
- Courses / self-paced content
- Learning plans, homework, assessments
- B2B organisations / tenancy
- Multi-currency
- WhatsApp notifications
- Meilisearch
