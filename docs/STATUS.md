# STATUS — cycle 04 r7 (CP3) — written 2026-09-23 23:02 — Context: 104.6k/200k (52%, auto-compacts at 84%; measured via `get_usage` — v1.2/R67)
Tests: full `composer test` (task `bc5cklusd`) **completed**: 1036/1036 passed, 4716 assertions, `ledger:verify` green — Pint and PHPStan passed, RTL grep passed. Unchanged this write — no code touched, only a git-housekeeping correction to `docs/STATUS.md`/`docs/CYCLE-LOG.md` (see §1). CI on PR #13 head `bc0ca0c` (the merge-`main`-in commit made to land that correction): **pending** (`mcp__ccd_pr__get_status`, checked 23:02) — re-running on the new head; last known-green was `51a1bd5`, and this write introduced no code change, only the merge commit. Advisor: 0 this write (a mechanical git-housekeeping correction, not an architecture/money/frozen-file/production judgment call — rule 11 does not require one) — cycle 04 total unchanged at 18. Review: 3a done (0 Medium/High); 3b done (14 findings, 1 FAIL(Low, scope), resolved by R70, merged); **3c: fresh-subagent adversarial review complete (`opus`, round 1 of cap 2) — 1 Medium + 4 Low FAIL findings (code/test), 1 Low FAIL (doc, fixed in-cycle) — R50's merge rule not met ("Any Medium+ finding... → PR stays open... programme halts") — PR #13 open, unmerged, halted for owner. Unchanged this write: the owner's plain `update` in chat carried no new PLAN.md ruling on Owner action 1 (rule 1: PLAN.md byte-identical to the committed r7, `git diff HEAD -- docs/PLAN.md` empty) — see CYCLE-LOG 22:54 START/NOTE.**

## §1 Git state
`main` at `12e035c` (pushed this write, `87d5135..12e035c`) — the docs-only STATUS/CYCLE-LOG refresh, correctly landed on `main` this time. Branch `cp/3c-booking` at `bc0ca0c` on `origin` (pushed this write, `51a1bd5..bc0ca0c`, 19 commits ahead of `origin/main`) — a "Merge branch 'main' into cp/3c-booking" commit picking up `12e035c`, matching this programme's established per-push merge pattern. PR #13 open against `main`, CI **pending** on the new head (re-checked via `mcp__ccd_pr__get_status` at 23:02; `mergeable: MERGEABLE`, `mergeStateStatus: UNSTABLE` while checks run — no code changed this write, so no reason to expect a different result than the last known-green run on `51a1bd5`). Working tree clean — no uncommitted changes; stash empty (the one working stash from this write's correction was dropped once its content was confirmed absorbed into both branches). PR #13's body unchanged this write (already carries the actual review verdict from the prior write).

**Self-made-and-corrected process defect this write (rule 12, "own mistakes in plain words"):** committed the STATUS/CYCLE-LOG refresh as `001c4fe` with `[skip ci]` directly onto `cp/3c-booking` instead of `main` — the same defect class already disclosed below for `b7f3520`/`5887c24`, but caught before push, so corrected rather than merely disclosed: reverted the commit, relocated its content to `main` via `git stash`/`checkout`/`checkout --theirs` (resolving the resulting conflict by taking the refresh content wholesale, since STATUS.md is a full rewrite each write and CYCLE-LOG.md is append-only), committed it to `main` as `12e035c` (correct target, `[skip ci]` correctly placed), then merged `main` back into `cp/3c-booking` (`bc0ca0c`) and re-pushed both. Full sequence and reasoning in CYCLE-LOG's `23:00` NOTE. No test, code, or ledger content was touched — docs-only, both branches now hold identical, correct content, and this instance (unlike the next paragraph's) needed no permanent disclosure-and-leave, since it was fully corrected before ever reaching `origin`.

**Disclosed process defects, both already corrected in CYCLE-LOG rather than left silent (rule 12):** (1) the branch's first-ever push this session (`cp/3c-booking` → `origin`) returned `* [new branch]`, contradicting an earlier STATUS/PLAN claim that it was already pushed — corrected via the 22:09 NOTE; (2) two commits on the feature branch, `b7f3520` and `5887c24`, wrongly carry `[skip ci]` (a message meant for docs-only commits to `main`, never for PR-branch commits) — history cannot be rewritten at this point without a force-push, which CLAUDE.md's Git Safety Protocol treats as needing explicit authorisation; disclosed here and left as-is, the recipe is corrected going forward (commit `7d088ac` onward on the branch omits it). Neither defect affected test results, money invariants, or the review's findings. PR #12 unchanged, merged. trustutor-rehearsal unchanged, behind `main` by design (R41).

## §2 Step map (cycle 04 r7 — CP3, an autonomous programme, R50)
1. `cp/3a-lows` — **merged** `b75d6e9` (PR #11).
2. `cp/3b-state-machine` — **merged** `5471bcb` (PR #12).
3. `cp/3c-booking` — **halted for owner GO, per R50/rule-6-item-6.** `BookLesson` action, `Payment` model, `ExpireUnpaidLessons` sweep, R74 concurrency test, R53 `Onboarding.vue` + booking-side test fixes, DATA_MODEL v1.4 — all built, committed, pushed, PR #13 opened, fresh-subagent adversarial review run on the stronger model (`opus`). Review found 1 Medium + 5 Low FAIL findings; 1 Low (doc-only, DATA_MODEL.md) fixed in-cycle this write; the Medium and the four remaining Lows (all in `app/` code or `tests/`) are **not** doc-only, so per rule 6 item 6 they stop and return to the owner rather than being attempted in-cycle. See §7 for the full punch list.
4. `cp/3d-cancellation` — not started, blocked behind 3c's merge.
5. `cp/3e-dashboards-emails` — not started.
6. Programme end and rehearsal deploy — not started — halt with the Deploy action.

## §3 What changed this run
- Sixth ADVISOR consult (prior sub-cycle write, `21:58`) surfaced four pre-PR defects, all fixed: a log-integrity correcting NOTE + an actual `git show` read-back; the DATA_MODEL v1.4 header self-contradiction fixed; a real booking-based R53 test added (`BookLessonTest.php`, band-minimum/band-maximum/odd-fils dataset, 3 new tests); the branch's `[new branch]` first push disclosed against stale STATUS/PLAN claims.
- PR #13 opened: https://github.com/rizwanoor80/eLearning-Platform/pull/13 — Summary + a 7-point Review brief quoting R53/R56/R74's literal PLAN text + disclosure of the known process defects (§1).
- Fresh-subagent adversarial review run twice this window: a first pass (foreground, model `opus`) reached a "fix before merge" verdict but its full findings list was not preserved through this session's own compaction boundary, only a prose summary survived; rather than log reconstructed "verbatim" findings, the review was re-run on the same unchanged PR head (`1dda465`) to capture genuine, complete, quotable output — logged as CYCLE-LOG's `22:40` REVIEW entry, round 1 of cap 2 (no fix was applied between the two passes, so no round was consumed by the re-run itself).
- **The review's one Medium finding, verified independently this window by reading the actual files** (not just trusting the subagent): `app/Actions/Lessons/BookLesson.php:158-169` only catches `LessonTransitionException` after a successful payment capture. `LedgerService::hold()` throws a different exception, `LedgerException`, when the price is ≤ 0 (`app/Services/Ledger/LedgerService.php:52-53`) — confirmed by reading the method directly. `LessonStateMachine::transition()` wraps the whole confirm step in one `DB::transaction()` (`app/Services/Lessons/LessonStateMachine.php:139`), so when `hold()` throws inside it, the transaction rolls back and the lesson's status update never commits — the lesson is left at `pending_payment`. The `Payment::create()` call that wrote `status: Captured` (`BookLesson.php:148-156`) happened in a separate, already-committed statement *before* this transaction, so it survives the rollback: a captured payment with no ledger entries, attached to a lesson stuck in `pending_payment`. Confirmed the trigger is live today: `app/Filament/Pages/ManageSettings.php:75` allows `trial_discount_pct` up to 100 via plain `minValue(0)->maxValue(100)`, and the PRD (`docs/PRD.md:45,100`) never states a ceiling below 100% — a 100% trial discount is not explicitly disallowed, so "reject a zero price" is a product-shape question, not a pure bug fix. **Also confirmed this window: `BookLesson` has no live caller today** — grepped `app(BookLesson::class)` / `BookLesson::class)` / `new BookLesson(` across the whole repo; every match is inside `tests/Feature/Lessons/BookLessonTest.php`. No controller or route wires it up yet, so no real user can reach this bug through the app as it stands today — this materially changes the owner's options (§7).
- Fixed in-cycle (Low, doc-only, `docs/DATA_MODEL.md`, review finding #14): header changelog "two real schema deviations" → "three", naming the `ledger_entries.account` enum's `gateway` value as Schema deviation 3; `### ledger_entries` column list and a new inline note add `gateway`; the Ledger-effect table's HOLD/RELEASE rows corrected to match `LedgerService.php:25-27` exactly; the trial partial-index bullet corrected from "cancelled states" to "freeing states" with the exact `LessonStatus::freeingSlotValues()` set spelled out. Read back via `git diff docs/DATA_MODEL.md` — five edits land exactly as intended, no stray changes.
- Full `composer test` re-queued in the background (task `bc5cklusd`) before the next push, per rule 7 — not yet complete at this write; Pint/PHPStan/RTL already green.

## §4 Decisions and by whom
- Owner/planner rulings carried, unchanged from last write: R70, R71, R72, R73.
- CC decisions carried from the design phase (unchanged): gateway resolution unbound/test-only (invariant 16, CP5-deferred), commission-split formula, tutor overlap via additive `EXCLUDE USING gist`, `tsrange` not `tstzrange`, `payments` row committed before `LessonStateMachine::transition()` runs, no learner-side overlap constraint in v1, a declined capture writes its own `failed` `payments` row before the lesson expires.
- Carried, R53 Option A over Option B (live-as-you-type onboarding preview removed) — unchanged this write, still a §6/§7 item for the owner to weigh.
- **New CC decision this write (advisor-consulted, logged in CYCLE-LOG as the `22:40` REVIEW entry's DECISION)**: findings #1 (Medium, code), #2/#3 (Low, `app/Console/Commands/ExpireUnpaidLessons.php`), #4/#5 (Low, test files) are not doc-only, so rule 6 item 6's "Medium+ or anything in code/config/routes/migrations/tests stops and returns to the owner" routes all five to the owner rather than an in-cycle fix attempt — no fix-loop round spent on them. Only #14 (Low, doc-only) was fixed in-cycle. This is a rule-text application, not a judgment call the advisor was asked to weigh in on differently.
- Advisor consulted once this write on the halt/verification approach (re-verify the Medium finding first-hand, check for live callers, check the PRD's stance on 100% trial discounts, re-run the review to get genuine quotable findings rather than log a reconstructed list) — all four instructions followed, see §3.

## §5 Why stopping
Still halted for the owner per R50's merge rule and CLAUDE.md rule 6 item 6 — **this is a halt for a GO, not the cycle's END** (rule 13: no §5 context handoff beyond this paragraph, no HANDOFF entry, no `/clear` Owner action). This write follows the same `update` as the prior write; the intervening work was purely a self-caught git-housekeeping correction (§1) with no bearing on the substantive gate — no new PLAN.md ruling arrived, and none was needed for a mechanical fix to which branch a docs-only commit landed on. The gate itself has not moved: the review found a Medium finding in code (`BookLesson.php`) plus four Low findings in code/tests that CLAUDE.md's rule 6 item 6 does not authorise fixing in-cycle, and R50's own merge rule is explicit: "Any Medium+ finding, any rule miss, or any broken money invariant (R52/R53) → PR stays open, STATUS §7 names it 'awaiting owner', programme halts." Per rule 8, nobody self-authorises R55's GO — a bare `update` in chat is not itself a ruling on Owner action 1's Option 1 vs. Option 2; that answer can only arrive via a new PLAN.md revision from the planner. The next `update` resumes mid-cycle, picking up from Owner action 1 below the moment PLAN.md carries a ruling on it. Context is 52% at this write (v1.2/R67: a context percentage is never itself a stop reason) — the halt is driven by the unmet gate, not by context.

## §6 Mismatches
- **New, not blocking merge, needs an owner call before the fix is written (§7 Owner action 1)**: the Medium finding's correct fix depends on a product decision CC cannot make unilaterally — is a 100%-discount (free) trial an allowed configuration? If yes, "reject zero price before capture" breaks a valid admin setting, and the real fix is either capping `trial_discount_pct` below 100, or building a distinct "free trial, no capture, no hold" code path. If no, capping the setting plus a pre-capture guard is enough. PRD §12/45/100 do not settle this either way.
- **New, non-blocking**: review finding #7 flagged a wrong comment (not a functional bug) in `tests/Unit/MoneyTest.php:40` — the comment's stated rationale is arithmetically inverted, though the test's own assertions are correct. Folds into the same fix-loop round as the Medium finding, not separately blocking.
- **New, non-blocking, needs an owner decision, not from this window's review but from the advisor's own reading of PRD vs DATA_MODEL v1.4**: `payments.lesson_id` is UNIQUE (a real v1.4 schema deviation, confirmed necessary as `BookLesson`'s idempotency guard) — but the PRD describes retrying a declined weekly recurring charge up to 3 times against the *same* `reserved` lesson (CP4/CP5 territory), which a UNIQUE `lesson_id` constraint on `payments` would block outright once that feature is built. Not urgent now (CP3 doesn't touch recurring charging), but the owner should rule on it now or explicitly defer to CP5, since the fix is a new migration either way (e.g. drop the uniqueness, add a partial unique on `(lesson_id) WHERE status = 'captured'` instead, or scope retries to new payment rows keyed by an attempt number).
- **New, non-blocking, needs an owner confirmation before R61 (rehearsal deploy)**: CI runs migrations as the Postgres superuser `root`, so a green CI run does not prove the actual `trustutor-rehearsal` database user can execute `CREATE EXTENSION IF NOT EXISTS btree_gist` (the new overlap-exclusion migration, `2026_09_23_100100_add_tutor_overlap_exclusion_to_lessons.php`). CLAUDE.local.md's allow-list has no read-only command that checks extension-creation privilege without running SQL, and raw SQL on rehearsal is on the never-list — this needs the owner (or the Forge dashboard) to confirm before the R61 deploy step runs, not before this merge.
- Carried, unchanged: `composer test`'s `"test"` script lacks `disableProcessTimeout`, worked around with `COMPOSER_PROCESS_TIMEOUT=1200` plus backgrounding from the start.
- Carried, unchanged: invariant 16 / plan-repo conflict (gateway unbound outside tests, registry deferred to CP5); rehearsal behind `main` by design (R41); branch protection on `main` not set (R23, Owner action A, non-blocking).
- Carried, unchanged: Owner action C (learner-side overlap constraint) and Owner action E (R53 live-preview UX loss) — see §7.

## §7 Next step / Owner actions
**Owner action 1 (blocking this cycle's continuation — R55/rule-6-item-6 GO).** The fresh-subagent adversarial review of PR #13 (round 1 of cap 2) found 1 Medium + 4 Low FAIL findings that are not doc-only, so they stop for the owner rather than being fixed in-cycle (full findings in CYCLE-LOG's `22:40` REVIEW entry):
- **#1 (Medium, `app/Actions/Lessons/BookLesson.php:158-169`)**: a captured payment can be stranded on a lesson permanently stuck at `pending_payment`, with no ledger entries — invisible to `ledger:verify` and excluded forever from the `ExpireUnpaidLessons` sweep's `whereDoesntHave(payment, Captured)` predicate. Reachable today via an admin setting `trial_discount_pct` to 100 (allowed by current validation, not explicitly disallowed by the PRD), and by any other exception thrown inside the post-capture confirm transition. **Verified this window: `BookLesson` has no live route/controller caller yet** — it is only invoked from `tests/`, so no real user can trigger this today.
- **#2/#3 (Low, `app/Console/Commands/ExpireUnpaidLessons.php:47,49`)**: the sweep doesn't re-check for a captured payment after taking its row lock (a capture racing the lock could get wrongly expired — low risk in CP3 since the fake gateway captures instantly; becomes real with CP5 gateway latency); and its `->each()` pagination skips rows past 1000 pending lessons in a single run (self-heals since the sweep runs every minute, but undercounts its own output).
- **#4 (Low, `tests/Feature/Ledger/LedgerServiceTest.php:272-277`)**: the R53 float/round()-on-money grep test doesn't scan the new 3c money files (`BookLesson.php`, `Money.php`, `Payment.php`, `Services/Payments/*`, `ExpireUnpaidLessons.php`, `TutorProfile.php`, `Onboarding.vue`) — a manual grep of those files today finds no actual float/round() violation, this is a coverage gap only.
- **#5 (Low, `BookLessonTest.php`/`TutorOnboardingBankSubjectsRateTest.php`)**: R53's "preview and booked price agree across the whole band table" is tested against a synthetic band + the one seeded Exam1 band, not all three seeded `LevelTier::band()` rows, and no single test directly cross-checks the Inertia prop against a booked lesson's price for the same tutor (agreement currently holds only by transitivity through two separate tests).

  **Option 1 (Recommended)** — authorise a fix-loop round (round 1 of cap 2) on this branch: add a price guard before capture shaped by the owner's answer to the "is a 100%-discount trial allowed" question above; catch `Throwable` (not just `LessonTransitionException`) after capture and move the lesson to a clearly-flagged state rather than leaving it stuck; use `lazyById()` in the sweep and add the post-lock re-check; widen the float-grep test to the new files; extend the R53 test to the full seeded band table with a single cross-check test; fix the `MoneyTest.php:40` comment. Then re-review, then this Owner action resolves to a plain R55 GO. Reason: closes a real (if not-yet-reachable) invariant-#1 risk before CP4/CP5 make it reachable via a live booking route, cheaply, while the fix-loop budget (cap 2, 0 rounds spent so far) is untouched.
  - **Option 2** — merge as-is; since `BookLesson` has no live caller today, no user can trigger #1 through the app yet. Carry #1–#5 as named blockers that must close before `cp/3d-cancellation` or any future work wires a real route to `BookLesson`. Viable only because of the "no live caller" finding above, but leaves a known money-invariant gap in `main` between now and whenever it's addressed.

  Reply `update` naming Option 1 or Option 2 (or a different instruction) to resume.

Non-blocking, does not gate Owner action 1 or this cycle's continuation:
- **Owner action 2 (recommended, non-blocking)**: `payments.lesson_id` UNIQUE vs. the PRD's CP4/CP5 "retry a declined weekly charge up to 3 times on the same reserved lesson" design (§6) — rule now, or explicitly defer to CP5 (a new migration is needed either way). **Recommended: defer to CP5, note it on the CP8/CP5 carry-forward list (Recommended)** — CP3 never exercises recurring charging, so ruling now buys nothing; reply `update` when reviewed.
- **Owner action 3 (recommended, non-blocking)**: confirm the `trustutor-rehearsal` Postgres user can run `CREATE EXTENSION IF NOT EXISTS btree_gist` before the R61 deploy step (CI's superuser run doesn't prove this). **Recommended: confirm via Forge/Hetzner dashboard before R61, not before this merge (Recommended)** — this migration hasn't reached rehearsal yet; reply `update` when reviewed.
- Carried, unchanged: Owner action C (learner-side overlap constraint) — **Recommended: leave as-is (Recommended)**.
- Carried, unchanged: Owner action E (R53 live-as-you-type preview UX loss, revisit at CP8) — **Recommended: leave as Option A for v1 (Recommended)**.
- Carried, optional: Owner action A (branch protection on `main`, R7); Owner action B (`.claude/settings.local.json`, ADR-002).

The standing clear action is **not** raised here — this is a halt for a GO mid-cycle, not the cycle's END (rule 13); v1.2/R67 clears only at cycle END.

## §8 Programme board — CP3 (R50)
| Sub-cycle | State | Branch | PR | Review verdict | Merge |
|---|---|---|---|---|---|
| 3a Lows pass | **merged** | `cp/3a-lows` | [#11](https://github.com/rizwanoor80/eLearning-Platform/pull/11) | 0 Medium/High | `b75d6e9` (R50) |
| 3b state machine + ledger | **merged** | `cp/3b-state-machine` | [#12](https://github.com/rizwanoor80/eLearning-Platform/pull/12) | 14 findings, 1 FAIL(Low, scope) — resolved by R70 | `5471bcb` (R50, item 12 closed by owner ruling) |
| 3c booking | **halted for owner GO** — review found 1 Medium + 5 Low FAIL (1 Low doc-only fixed in-cycle); awaiting Owner action 1 | `cp/3c-booking` | [#13](https://github.com/rizwanoor80/eLearning-Platform/pull/13) | 1 Medium + 5 Low FAIL, round 1 of cap 2 — **not merged, R50 merge rule not met** | — |
| 3d cancellation | not started | `cp/3d-cancellation` | — | — | — |
| 3e dashboards, emails, deletion | not started | `cp/3e-dashboards-emails` | — | — | — |
| Programme end + rehearsal deploy | not started — halt | — | — | — | — |

Resume count: **0 of 8** (unchanged — this halt is for an owner GO under R50's merge rule, not a stop condition/resume per R69).

## Deferred (out of v1 scope — do not build)
- Lesson packs / subscriptions / credits
- Group classes
- Courses / self-paced content
- Learning plans, homework, assessments
- B2B organisations / tenancy
- Multi-currency
- WhatsApp notifications
- Meilisearch

## Carried to CP8 hardening checklist (R71)
- Search pagination: tutor search pages in memory after the slot check; revisit at "a few hundred approved tutors" (same trigger as the Meilisearch item).
- Refunding a captured-but-expired lesson (the payments-row-before-transition race) needs the real payment gateway — `FakePaymentGateway` cannot refund. Revisit when CP5 wires a real driver.
- The R53 onboarding live-as-you-type trial-price preview (Owner action E) — revisit if the UX loss is judged to matter.
- **New this write**: review finding #2/#3 (sweep post-lock re-check, `lazyById()` pagination) — Low risk today, real risk at CP5 gateway latency; revisit then regardless of Owner action 1's outcome.
